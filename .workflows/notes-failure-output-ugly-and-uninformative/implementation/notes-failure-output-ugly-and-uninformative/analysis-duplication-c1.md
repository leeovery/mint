AGENT: duplication
FINDINGS:
- FINDING: Three near-identical abort-chain test helpers in the engine package
  SEVERITY: medium
  FILES: internal/engine/notesfailureoutput_internal_test.go:32-39, internal/engine/notesfailurewiring_internal_test.go:75-82, internal/engine/failuremessage_internal_test.go:27-34
  DESCRIPTION: Three helpers — resolveAbortAround (notesfailureoutput, T2-2), wrapAsAbort (notesfailurewiring, T2-3), and resolveAbortError (failuremessage, T2-1) — have byte-identical bodies. Each is a t.Helper() that calls notes.ResolveFailure(t.Context(), runner.NewFakeRunner(), <arg>, "v1.0.0", config.Release{OnNotesFailure: "abort"}), fatals on a nil error, and returns err. Even the doc comment is the same ("...wraps ... through abortError (\"notes generation failed (%s): %w\") ... The git runner is never invoked in abort mode."). Only the function name and the parameter label (carrier / failure / sentinel) differ. All three live in package engine (internal tests), so they are the exact same construct authored three times across three task boundaries — copy-paste drift waiting to happen: the magic literals "v1.0.0" and OnNotesFailure: "abort" must now be kept in sync across three sites, and a future change to ResolveFailure's signature touches all three identically.
  RECOMMENDATION: Extract one shared helper in the engine internal-test package (e.g. an engine_failure_testhelpers_test.go, or hoist into whichever of the three files reads most naturally) — a single func wrapNotesAbort(t *testing.T, cause error) error — and have all three callers use it. This is a pure de-duplication of existing test code (no new behavior), and consolidating it also collapses the three duplicated doc comments into one source of truth for the abort-chain shape.

- FINDING: Duplicated concise-message assertion triplet in failuremessage test
  SEVERITY: low
  FILES: internal/engine/failuremessage_internal_test.go:63-71, internal/engine/failuremessage_internal_test.go:90-98
  DESCRIPTION: The three-part guard asserting the concise phrase has no nested %w chain (no ":"), no "failed", and no leading "notes" label appears verbatim twice — once inside the table loop of TestFailureMessage_CollapsesForwardAbortChain and again as standalone statements in TestFailureMessage_CollapsesRegenerateShortChain. Identical strings.Contains/HasPrefix checks and identical t.Errorf messages. This is the literal encoding of Acceptance Criteria #2 written in two places, so a change to the rule (e.g. tightening what counts as "the stage label") must be edited in both.
  RECOMMENDATION: Extract a single helper, e.g. assertConcisePhrase(t *testing.T, got string), holding the three checks, and call it from both tests. Small but worthwhile because it pins one AC in one place; defer only if the team prefers inline assertions for these two call sites.

- FINDING: GenerationError carrier literal built in two transport branches (re-judged)
  SEVERITY: low
  FILES: internal/ai/transport.go:208, internal/ai/transport.go:219
  DESCRIPTION: Generate builds &GenerationError{Stdout: res.Stdout, Stderr: res.Stderr, ExitCode: res.ExitCode} in two separate post-retry branches (the non-zero-exit path and the empty/whitespace-body path). The literals are identical. A prior reviewer deemed this acceptable; re-judged independently I concur it is NOT worth extracting. The two branches are reached via genuinely distinct control flow (err != nil vs !isValid(res.Stdout)) with distinct, load-bearing WHY-comments explaining why each shape packs the same carrier; a shared local closure or helper would add an indirection that obscures the deliberate unification the comments call out, and the literal is a 3-field struct, not a 20-line block. Recording it only to close the loop the input asked for.
  RECOMMENDATION: No action. Leave both literals inline; the per-branch comments are the value here and a helper would cost more clarity than the three repeated field assignments save.

- FINDING: notesFailureOutput / hookFailureOutput parallel helpers (re-judged)
  SEVERITY: low
  FILES: internal/engine/release.go:1598-1604, internal/engine/release.go:1622-1639
  DESCRIPTION: notesFailureOutput deliberately mirrors hookFailureOutput's typed-carrier-extraction PATTERN (per spec Fix 1 step 3) but reads STDOUT and composes two streams, whereas hookFailureOutput reads a single Result.Stderr. Re-judged independently: the shared shape is only the errors.As(cause, &typed) guard returning "" on no-match — about two lines — after which the bodies diverge substantively (single-field copy vs two-stream emptiness-trim-join-trimRight composition). The carrier types are distinct (*hooks.HookError holds a whole runner.Result; *ai.GenerationError holds split Stdout/Stderr/ExitCode fields), so no common interface exists to extract against without inventing one (which would be a new abstraction, out of scope). This is parallel-but-distinct, correctly kept as two helpers.
  RECOMMENDATION: No action. The parallel is intentional, documented, and the divergent bodies justify two helpers over a forced shared abstraction.
SUMMARY: One medium finding worth acting on — three byte-identical abort-chain test helpers (resolveAbortAround / wrapAsAbort / resolveAbortError) in the same engine internal-test package should collapse to one shared helper, plus a small duplicated concise-message assertion triplet. The two transport-level patterns flagged in the input (the dual GenerationError literals and the notesFailureOutput/hookFailureOutput parallel) are correctly left as-is on independent re-judgement.
