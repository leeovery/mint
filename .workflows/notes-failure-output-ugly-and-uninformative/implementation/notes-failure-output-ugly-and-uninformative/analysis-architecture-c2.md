AGENT: architecture
FINDINGS:
- FINDING: GenerationError.Error()/ExitCode contract is wrong for the empty/whitespace-body construction site
  SEVERITY: low
  FILES: internal/ai/transport.go:56-69, internal/ai/transport.go:208, internal/ai/transport.go:219
  DESCRIPTION: GenerationError carries `ExitCode int` whose field doc states it is "the NON-ZERO process exit status that classified this as bad content", and Error() renders `fmt.Sprintf("ai generation failed (exit %d)", e.ExitCode)`. The type has two construction sites: the non-zero-exit path (line 208), where ExitCode is genuinely non-zero, and the empty/whitespace-body-after-retry path (line 219), which is reached on a CLEAN (zero-exit) attempt whose body merely failed isValid. On that second path ExitCode is 0, so Error() renders the self-contradicting string "ai generation failed (exit 0)" — a zero exit labelled as the exit that classified the failure. The field annotation ("non-zero") is therefore false for one of the type's two legitimate inputs. This is a latent diagnostic-quality issue rather than a user-facing correctness bug: in the rendered failure path notes.CauseText intercepts the carrier first (failureMessage / abortError both resolve it to the concise phrase before Error() is reached), so Error() only surfaces in logs and the defensive failureMessage fallback. But the contract is still internally inconsistent — a struct field documented as one thing while a real caller supplies the opposite — which is exactly the kind of tag/annotation that produces a wrong result if reused outside its current narrow display-suppressed context.
  RECOMMENDATION: Make the carrier honest about its dual provenance. Either (a) widen the field doc to acknowledge both shapes (non-zero exit OR a zero-exit empty/whitespace body) and have Error() distinguish them (e.g. omit "(exit 0)" when ExitCode==0, or render an "empty body" variant), or (b) drop ExitCode from the empty/whitespace site (it is unused downstream — notesFailureOutput reads only Stdout/Stderr) and document that ExitCode is meaningful only on the non-zero-exit path. Keep the change scoped to the Error() string and the field doc; do not alter the routing or the carried Stdout/Stderr.
SUMMARY: The carrier upgrade, the CauseText/failureMessage display-derivation seam, the notesFailureOutput composition helper, and the test coverage (transport carrier shapes, white-box wiring, and a true end-to-end proof through the production transport+spine) compose cleanly across task boundaries and preserve every load-bearing AI-seam invariant. The one architectural blemish is the GenerationError.ExitCode/Error() contract, which is accurate for the non-zero-exit site but self-contradicting ("exit 0") for the empty/whitespace-body site that shares the same struct.
