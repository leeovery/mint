AGENT: duplication
FINDINGS:
- FINDING: Byte-identical StageFailed extraction helper twinned across the package boundary
  SEVERITY: low
  FILES: internal/engine/notesfailurewiring_internal_test.go:57-73, internal/engine/release_priortag_test.go:790-806
  DESCRIPTION: onlyStageFailure (white-box package engine) and onlyStageFailureEvent (external package engine_test) are functionally byte-identical: both walk rec.Events, fail on more-than-one or zero KindStageFailed, and return the single recorded presenter.StageFailure. They were authored independently for the two surfacing-path proofs added in this work unit. Both carry a head-comment acknowledging the twin and instructing "edit BOTH together" — so the divergence risk is documented rather than latent. The duplication is genuine but structurally unavoidable in Go: the package engine copy is unexported and invisible to engine_test, and exporting a test helper into the production package is forbidden by the project test idioms. Flagged for completeness so the audit shows the twin was seen and judged, not missed.
  RECOMMENDATION: Leave as-is — no consolidation. The only collapse points (a shared exported helper in package engine, or moving one test across the package boundary) are both ruled out by project conventions and are out of scope for this work unit. The cross-reference comments on both copies already discharge the maintenance hazard; keep them in sync if either is touched.
SUMMARY: The implementation's shared logic is already consolidated to single sources of truth (notes.CauseText feeds both the abort message and the engine display helper; wrapNotesAbort/assertConcisePhrase were lifted into one engine_failure_testhelpers_test.go file this cycle; notesFailureOutput deliberately mirrors hookFailureOutput's pattern with a spec-mandated different field choice). The one remaining cross-file duplicate — the byte-identical onlyStageFailure/onlyStageFailureEvent twin — is an unavoidable consequence of the white-box/external package split and is already documented with paired edit-together comments. No actionable consolidation remains.
