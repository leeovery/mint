AGENT: duplication
FINDINGS:
- FINDING: Two byte-identical "extract the single StageFailed payload" helpers across the engine test packages
  SEVERITY: medium
  FILES: internal/engine/notesfailurewiring_internal_test.go:50-66, internal/engine/release_priortag_test.go:782-798
  DESCRIPTION: onlyStageFailure (package engine, internal test, added by T2-3) and onlyStageFailureEvent (package engine_test, external test, added by T3-2) have functionally identical bodies. Both take (t *testing.T, rec *presentertest.RecordingPresenter), loop rec.Events, match `rec.Events[i].Kind == presentertest.KindStageFailed`, t.Fatalf on a second match ("recorded more than one StageFailed"), capture rec.Events[i].StageFailed into a found pointer, t.Fatalf when none was recorded, and return *found. The ONLY differences are the function name, the local capture variable (`f` vs `sf`), and trivial wording in the fatal messages. They were authored independently across two task boundaries (T2-3 wiring, T3-2 end-to-end) — a textbook copy-paste-drift candidate: a change to the RecordingPresenter event shape, or to the "exactly one StageFailed" contract, must now be edited in two places that can silently diverge. (Note: the two helpers sit in DIFFERENT compilation units — package engine vs package engine_test — so a single shared func is not a one-line move; see recommendation.)
  RECOMMENDATION: Consolidate to one helper used by both call sites. The cleanest home is the existing shared engine-test helper file added in T3-1 (engine_failure_testhelpers_test.go, currently package engine). Since that file is the internal-test package and release_priortag_test.go is the external engine_test package, either (a) make the single helper available to both by placing it in a small *_test.go file that the external package can call (e.g. promote it next to the other shared engine_test helpers in the external package and have the internal wiring test call it — confirm package visibility), or (b) if the package split makes a single source impractical, collapse at least each package's own copy and add a short comment cross-referencing the twin so the duplication is intentional and documented rather than accidental. This is pure de-duplication of existing test scaffolding (no behaviour change).

- FINDING: GenerationError carrier literal built in two transport branches (re-judged, carried from c1)
  SEVERITY: low
  FILES: internal/ai/transport.go:208, internal/ai/transport.go:219
  DESCRIPTION: Generate builds &GenerationError{Stdout: res.Stdout, Stderr: res.Stderr, ExitCode: res.ExitCode} in two distinct post-retry branches (the non-zero-exit path at line 208 and the empty/whitespace-body path at line 219). The struct literals are identical. Re-judged independently this cycle: the two branches are reached via genuinely different control flow (err != nil after classifyFatal vs !isValid(res.Stdout)) and each carries a load-bearing WHY-comment explaining why the same carrier is packed for two different bad-content shapes. The repeated literal is a 3-field struct, not a sizeable block; a shared closure/helper would add indirection that obscures the deliberate unification the comments call out. Concurs with the c1 re-judgement.
  RECOMMENDATION: No action. Leave both literals inline; the per-branch comments are the value and a helper would cost more clarity than the three repeated field assignments save.

- FINDING: notesFailureOutput / hookFailureOutput parallel extraction helpers (re-judged, carried from c1)
  SEVERITY: low
  FILES: internal/engine/release.go:1598-1604, internal/engine/release.go:1622-1639
  DESCRIPTION: notesFailureOutput deliberately mirrors hookFailureOutput's typed-carrier-extraction PATTERN (mandated by spec Fix 1 step 3) but reads STDOUT and composes two streams, whereas hookFailureOutput reads a single Result.Stderr. The shared shape is only the `errors.As(cause, &typed)` guard returning "" on no-match (~2 lines); after that the bodies diverge substantively (single-field copy vs two-stream emptiness-trim / stdout-first join / TrimRightFunc). The carrier types are distinct (*hooks.HookError holds a whole runner.Result; *ai.GenerationError holds split Stdout/Stderr/ExitCode), so no common interface exists to extract against without inventing a new abstraction (out of scope, and a new-feature risk). Parallel-but-distinct, correctly two helpers. Concurs with the c1 re-judgement.
  RECOMMENDATION: No action. The parallel is intentional and documented; the divergent bodies justify two helpers over a forced shared abstraction.

- FINDING: Repeated four-sentinel -> concise-phrase test table across five test files (re-judged)
  SEVERITY: low
  FILES: internal/engine/failuremessage_internal_test.go:30-35, internal/engine/notesfailurewiring_internal_test.go:121-129, internal/notes/causetext_test.go:19-28, internal/engine/notesfailureoutput_internal_test.go:133-141
  DESCRIPTION: The mapping from the four notes-failure sentinels to their concise phrases ({ai.ErrTimeout -> "AI timed out"}, {notes.ErrDiffTooLarge -> "diff too large"}, {ai.ErrCommandMissing -> "AI tool not installed"}, {ai.ErrGenerationFailed -> "AI returned empty/invalid notes after retry"}) is restated as a case table in several tests. Re-judged: this is expected, healthy test data — each table pins the SAME contract from a DIFFERENT angle (CauseText derivation in notes; failureMessage collapse, Output emptiness, and surfacing-wiring in engine) and the tables are NOT identical (some carry only the subset they need, e.g. notesfailureoutput drops the phrase entirely and asserts "", notesfailurewiring adds a per-case message field). Hoisting the literals into one shared fixture would couple tests across two packages and weaken each test's self-contained, exact-value assertion — which the project's test idioms explicitly favour. The single SOURCE-of-truth for the mapping is notes.CauseText (the production code); the tests legitimately restate expected values.
  RECOMMENDATION: No action. Restating expected literals per test is the intended pattern here; do not extract a shared fixture.
SUMMARY: One actionable medium finding: the two byte-identical single-StageFailed-extraction helpers (onlyStageFailure in package engine, onlyStageFailureEvent in package engine_test) added across the T2-3 and T3-2 task boundaries should be consolidated. The three lower-severity patterns (dual GenerationError literal, the notesFailureOutput/hookFailureOutput parallel, and the repeated sentinel->phrase test tables) are correctly left as-is on independent re-judgement.
