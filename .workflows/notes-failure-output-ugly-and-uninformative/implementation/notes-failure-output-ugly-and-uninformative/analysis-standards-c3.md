AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. All five acceptance criteria are met (StageFailure.Output populated per the settled composition rule; concise top-line Message with no nested %w chain, no leading stage label, no "failed"; both surfacing paths render identically; padStage gap unchanged); all five AI-seam invariants are preserved (errors.Is(ErrGenerationFailed) still matches via Unwrap, context.Canceled propagates unchanged and uncarried, transport never imports config, single-retry ownership intact, byte-identical success path); and all project gates pass (go build, gofmt -l . empty, go vet, go test -race ./..., golangci-lint 0 issues).

NOTES (verification detail, not drift):
- Fix 1 (transport carrier): ai.GenerationError wraps ErrGenerationFailed (Unwrap), carries Stdout/Stderr/ExitCode as distinct fields, populated only after the retry on both bad-content shapes (non-zero exit and empty/whitespace body); fatal causes (timeout, missing tool, cancel) short-circuit via classifyFatal and never pick up the carrier — all pinned by transport_test.go carrier guards.
- Fix 1 (engine composition): notesFailureOutput uses errors.As (traverses both the forward abortError chain and regenerate's shorter "generating notes: %w" chain), composes stdout-first then stderr with single-newline join, trims each stream only for the emptiness check, preserves interior content verbatim, trims only trailing whitespace — exactly the settled rule. Helper is called for every stage and returns "" for non-carrier causes, so only the AI generation-failure carrier populates Output.
- Fix 2 (concise message): failureMessage funnels surface, surfaceAndUnwind, and resetAndAbort; the new notes.CauseText branch (errors.Is-based, single source shared with the unexported causeText) collapses the four sentinels to concise phrases while leaving the %w chain intact for routing/logs. resetAndAbort inherits the concise Message for free and keeps an empty Output (its cause is never the AI carrier) — the third StageFailure builder is accounted for, as the spec required.
- Fix 3 (padStage gap): padStage is unchanged; the only pretty_test.go edit is the message text ("claude failed" -> the concise phrase). gate_forbidden_test.go and askline_test.go are untouched by this work unit.
- Scope adherence: the batch --all reportSkip / classifyNotesFailure path is correctly left untouched (classifyNotesFailure still returns reasonNotesFailed and carries no Output) — the two documented residual limitations are deliberate per the spec. commit's isAITransportFailure routing still keys on errors.Is(ErrGenerationFailed), so the editor fallback is unaffected by the carrier.
- Testing requirements: every required new test is present (transport non-zero-exit-with-stdout carrier + Canceled passthrough; engine/notes wiring populating Output; concise message; both surfacing paths) plus an end-to-end production-transport test driving the full two-wrap chain (release_priortag_test.go). The cross-package test-helper duplication (onlyStageFailure / onlyStageFailureEvent) is honestly documented at both sites with the reason it cannot be collapsed.
