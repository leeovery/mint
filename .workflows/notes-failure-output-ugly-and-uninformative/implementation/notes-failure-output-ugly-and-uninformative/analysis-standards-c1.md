AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. All five spec acceptance criteria are met (StageFailure.Output composed by the settled stdout-then-stderr/single-newline/trailing-trim rule; concise top-line Message via failureMessage→notes.CauseText with no nested %w chain, no leading stage prefix, no "failed"; both surfacing paths — forward surfaceAndUnwind and regenerate surface — wired identically through notesFailureOutput; padStage gap untouched with gate_forbidden_test.go and askline_test.go unmodified; and all AI-seam invariants preserved — errors.Is(ErrGenerationFailed) matches via GenerationError.Unwrap, context.Canceled propagates unchanged with carrier guards, internal/ai imports no config, single-retry ownership intact, byte-identical success path pinned). All project gates pass: go build, gofmt (clean), go vet, go test -race ./... (all pass), golangci-lint (0 issues).

NOTES (conformance confirmations, not drift):
- Fix 1 carrier: ai.GenerationError wraps ErrGenerationFailed (Unwrap) and holds Stdout/Stderr/ExitCode as distinct fields, mirroring *hooks.HookError's typed-carrier pattern (transport.go:43-73). Both bad-content shapes (non-zero exit and empty/whitespace surviving retry) converge on the SAME carrier (transport.go:208,219), per the spec's "unified, not divergent" rule. Captured output is packed only after the single retry is exhausted (Invariant 4).
- Fix 1 step 3 helper: notesFailureOutput uses errors.As (traverses %w), reads STDOUT not Result.Stderr (the deliberate field-choice divergence from hookFailureOutput), trims each stream only for the emptiness decision, includes stdout-then-stderr single-newline-joined, keeps interior content verbatim, and TrimRightFunc-trims the composed result (release.go:1606-1639). This matches the authoritative settled rule in Fix 1 step 3 exactly (the more terse "surrounding whitespace" phrasing in Acceptance Criterion #1 defers to that settled rule).
- Fix 2 display seam: failureMessage funnels all three StageFailed sites (surface, surfaceAndUnwind, resetAndAbort) and derives the concise phrase via notes.CauseText for the four known sentinels while leaving the %w chain intact for errors.Is/logs (release.go:1660-1683). resetAndAbort inherits the concise Message for free and is correctly out of scope for Output-population (its cause is a git failure; notesFailureOutput returns "" for it). The GenerationError.Error() "exit N" string never reaches display because carrier causes resolve through CauseText, not cause.Error().
- Fix 3: presenter StageFailed/padStage unchanged (pretty.go:536-546); the only pretty_test.go change is the single concise-message text update (claude failed → AI returned empty/invalid notes after retry), per the spec's test-impact note. gate_forbidden_test.go and askline_test.go are untouched.
- CauseText/causeText DRY: the exported CauseText is the single source; the unexported causeText (abort-message naming) and the engine's failureMessage both delegate to it, so the abort-message text and the display phrase cannot drift (resolve.go:104-140).
- AI-seam invariants pinned by explicit carrier-guard tests for timeout, missing-tool, cancel (both deadline and no-deadline paths), and the success path (transport_test.go), satisfying CLAUDE.md seam #5 and the spec Invariants section.
- Comment hygiene (CLAUDE.md "Comments"): the heavy WHY-comments on the new code state the contracts and the deliberate hook-helper-pattern-but-not-field divergence; they are true to as-built.
