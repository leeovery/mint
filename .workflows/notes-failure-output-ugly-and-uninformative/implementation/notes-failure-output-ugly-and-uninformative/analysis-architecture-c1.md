AGENT: architecture
FINDINGS:
- FINDING: No end-to-end test proves the carrier survives the real transport→generator→resolve→surface chain
  SEVERITY: medium
  FILES: internal/engine/release_priortag_test.go:682-715, internal/engine/notesfailurewiring_internal_test.go:75-129, internal/engine/notesfailureoutput_internal_test.go:32-39
  DESCRIPTION: The load-bearing payoff of this fix is that claude's captured output composed by the transport actually reaches StageFailure.Output through the full release spine. Every new test exercises the seam in ISOLATION: the wiring/output tests hand-construct an *ai.GenerationError and either call notesFailureOutput/failureMessage directly or wrap it through ResolveFailure (wrapAsAbort) before invoking surfaceAndUnwind/surface. None drive the production chain transport.Generate (packs carrier) → generator.generateFromDiffWithContext ("generating notes: %w", generate.go:185) → SelectBody/ResolveFailure (abortError, resolve.go:101) → surfaceAndUnwind. The one test that DOES run that whole chain end-to-end over the real production transport (newDeps leaves Transport nil) — TestRelease_PriorTag_NotesFailureAbort_AbortsBeforeMutation — seeds an EMPTY stdout (`runner.Result{Stdout: ""}`) so there is no output to carry, and asserts only that a StageFailed event was recorded; it never inspects StageFailure.Output or Message. Consequently the production chain shape is unverified end-to-end: in production the carrier sits TWO wraps deep (abortError → "generating notes: %w" → carrier), whereas the white-box wrapAsAbort builds a ONE-wrap chain (abortError → carrier). errors.As is depth-agnostic so the helper is correct, but a regression in the generator's wrap layer, the SelectBody plumbing, or the seeding contract (the very gap the spec calls out: "the prior fakes had no stdout to lose") would not be caught by any test. This is the exact integration seam the spec's Testing Requirements flagged as "the gap the existing tag/push-only presenter test left uncovered; assert at the wiring level, not just the presenter."
  RECOMMENDATION: Extend the existing release-level test (or add a sibling) that drives engine.Release with the real production transport (Transport nil) and seeds claude with a non-zero exit carrying stdout (e.g. runner.Result{Stdout: "Prompt is too long", ExitCode: 1} on both attempts), then asserts the recorded StageFailed payload has Output == "Prompt is too long" and Message == "AI returned empty/invalid notes after retry". This closes the loop from transport capture to rendered Output across the real seam composition the white-box tests deliberately bypass, at negligible cost (the fake-seeding harness already exists in release_priortag_test.go).
SUMMARY: The carrier/CauseText/notesFailureOutput seams are well-scoped and compose cleanly (minimal export surface, no drift, correct layering, content-agnostic transport, deferred items correctly untouched); the one architectural gap is that the load-bearing capture-to-render path is only proven by isolated white-box helper tests, never end-to-end through the real transport→generator→resolve→surface chain that production uses.
