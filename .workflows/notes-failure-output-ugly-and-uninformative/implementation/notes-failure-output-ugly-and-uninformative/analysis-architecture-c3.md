AGENT: architecture
FINDINGS:
- FINDING: Identical GenerationError construction duplicated across both Generate bad-content sites
  SEVERITY: low
  FILES: internal/ai/transport.go:220, internal/ai/transport.go:231
  DESCRIPTION: The two bad-content survival paths in Generate (non-zero-exit retry and empty/whitespace-body retry) each construct the carrier with the byte-identical expression `&GenerationError{Stdout: res.Stdout, Stderr: res.Stderr, ExitCode: res.ExitCode}` from the same `res`. This is a small instance of the parallel-construction smell the code-quality "Compose, Don't Duplicate" principle targets: a future field added to GenerationError (or a change to which res fields it captures) must be edited in two places that are required to stay identical. The carrier deliberately holds Stdout/Stderr/ExitCode as distinct fields rather than embedding the whole runner.Result (the spec-settled choice, justified by keeping stream composition downstream), so this is not a layering or correctness defect — both call sites already feed the carrier the same three res fields, so the mapping res→carrier is a single concept expressed twice. Worth noting at low severity only because it is exactly two sites (below the Rule-of-Three threshold) and the surrounding comments are explicit that they must mirror.
  RECOMMENDATION: Optionally fold the res→carrier mapping into one tiny constructor (e.g. `newGenerationError(res runner.Result) *GenerationError`) so the field list lives once; or leave as-is given it is only two sites and the duplication is intentional and well-commented. Not worth restructuring if the two-site mirror is judged acceptable.
SUMMARY: Architecture is sound — the AI-seam carrier upgrade preserves content-agnosticism and errors.Is routing, Fix 2's concise-phrase derivation is correctly composed (causeText delegates to the exported CauseText so the two cannot drift), notesFailureOutput mirrors the established hookFailureOutput typed-carrier pattern at the correct seam, and an end-to-end test pins the carrier surviving the full transport→generator→ResolveFailure→surfaceAndUnwind %w chain. The only observation is a minor, deliberate two-site duplication of the GenerationError construction.
