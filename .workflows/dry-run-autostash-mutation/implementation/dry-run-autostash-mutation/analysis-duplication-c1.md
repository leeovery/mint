AGENT: duplication
FINDINGS:
- FINDING: Two new RunLocalGates skipCleanTree tests are near-identical; the second is a subset of the first
  SEVERITY: low
  FILES: internal/preflight/preflight_test.go:262-288 (TestRunLocalGates_SkipCleanTree_SkipsCleanTreeGate), internal/preflight/preflight_test.go:290-304 (TestRunLocalGates_SkipCleanTree_DirtyTreeDoesNotAbort)
  DESCRIPTION: Both tests added in this change build the byte-identical argRunner (the same two responses: "rev-parse --abbrev-ref HEAD" and "rev-parse -q --verify refs/tags/v1.2.3") and make the identical RunLocalGates(ctx, r, "main", "v1.2.3", false, true) call. The second test's behaviour — "a dirty tree does not abort under skipCleanTree=true" — is already fully exercised by the first: because no "status --porcelain" response is seeded and the clean-tree gate is skipped, the run reaching a nil pass IS the proof that no dirty status was read and no abort occurred. The second test asserts strictly less than the first (it drops the call-count and porcelain-absence checks) over the same fixture, so it adds coverage of nothing the first does not already prove.
  RECOMMENDATION: Consolidate into one test (keep TestRunLocalGates_SkipCleanTree_SkipsCleanTreeGate, which already asserts the porcelain probe is absent, the run passes, and the call count is exactly the on-branch + tag-free probes) and drop TestRunLocalGates_SkipCleanTree_DirtyTreeDoesNotAbort, OR — if the "dirty tree" framing is wanted for documentation — have the dirty-tree test add the one distinguishing assertion the first lacks rather than re-running the same fixture for a weaker check. This is low severity and borderline against the Rule of Three (only two small ~12-line blocks); flagged for proportionality, not as a blocker.
SUMMARY: The implementation is a tightly-scoped quick-fix that correctly composes on the existing --any-branch gate-skip pattern (threading a skipCleanTree bool through RunLocalGates/runPreflight that mirrors anyBranch) rather than authoring parallel logic, so there is no production-code duplication. The only duplication is a pair of new preflight tests where the second is a strict subset of the first over an identical fixture.
