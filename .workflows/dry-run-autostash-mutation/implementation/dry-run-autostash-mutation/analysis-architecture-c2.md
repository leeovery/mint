AGENT: architecture
FINDINGS:
- FINDING: RunLocalGates is accreting per-gate skip booleans rather than a single gate-policy value
  SEVERITY: low
  FILES: internal/preflight/preflight.go:81, internal/engine/release.go:1106, internal/engine/release.go:413
  DESCRIPTION: RunLocalGates now takes two adjacent skip booleans (anyBranch, skipCleanTree) and runPreflight threads both through. The signature pattern is "one bool per gate that can be bypassed" — each new escape hatch that weakens a local gate adds another positional bool at both the preflight API and the engine seam. At the call site the meaning collapses into a positional `(…, opts.AnyBranch, opts.DryRun && opts.AutoStash)` pair where the two trailing booleans are easy to transpose (both are skip-this-gate flags of identical type). This is the boolean-parameter / growing-positional-list smell from code-quality.md; it is currently only mildly felt (two flags) but the structure invites the next escape hatch to extend it again. NOTE: this is a pre-existing pattern the quick-fix faithfully mirrored — the spec explicitly scoped it as "mirror the existing anyBranch parameter", so this is a latent structural observation, not a defect introduced by poor local judgement. Flagging only because the second instance is the point at which a named policy (e.g. a small skip-set or a gate-options struct) would start to pay off and prevent positional-transposition bugs.
  RECOMMENDATION: Acceptable to leave as-is for a two-flag quick-fix given the strong precedent and the tightly-scoped spec. If a third gate-skip ever lands, collapse the trailing booleans into a single typed value (e.g. a SkipGates set or a LocalGateOptions struct with named CleanTree/OnBranch fields) so each bypass is named at the call site and transposition becomes impossible. No change needed now.
SUMMARY: The quick-fix is architecturally sound — it surgically mirrors the established anyBranch gate-skip pattern across the preflight API, the runPreflight seam, and the engine call site, with clean comments and matching integration + unit coverage (dry-run+autostash dirty-tree previews mutation-free; non-autostash dirty dry run still aborts; real autostash unchanged). The only observation is the low-severity accretion of per-gate skip booleans on RunLocalGates, which is a pre-existing pattern faithfully followed, not a new flaw.
