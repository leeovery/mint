AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. Every spec decision is honoured: the autostash push/pop block is gated on `opts.AutoStash && !opts.DryRun` (release.go:392) so neither mutation reaches the Mutator in dry-run; the clean-tree gate is bypassed exactly for the `DryRun && AutoStash` combo via a `skipCleanTree` parameter threaded `Release → runPreflight → RunLocalGates` (release.go:413, release.go:1106, preflight.go:81), mirroring the existing `anyBranch` pattern as the spec directed. All required WHY-comments were updated and are true to as-built (AutoStash/DryRun field docs, the Stage-2 block comment, the Release doc, runPreflight doc, RunLocalGates doc, and the autostash.go header). All spec exclusions hold: the real-run autostash path, the non-autostash dirty dry-run abort, the other gates, and the notes diff/cache/body are untouched — no new flags, config keys, or presenter events were added. Every spec verification item has a corresponding test (engine: zero stash push/pop + no clean-tree probe + completed preview in TestRelease_DryRunAutostash_DirtyTree_NoStashMutation_CompletesPreview; regression guard TestRelease_DryRun_NonAutostash_DirtyTreeStillAborts; preflight: TestRunLocalGates_SkipCleanTree_SkipsCleanTreeGate with skipCleanTree=false paths unchanged). All five project gates pass independently (go build, gofmt -l, go vet, go test -race ./..., golangci-lint run = 0 issues).

NOTE (not a finding): `RunLocalGates` now carries two boolean parameters (`anyBranch, skipCleanTree`) and six params total, which the generic code-quality.md flags as a boolean-parameter / long-parameter-list anti-pattern. This is NOT drift: the spec explicitly mandated mirroring the established `anyBranch` boolean to keep one consistent gate-skip pattern across the preflight seam, and the spec-vs-convention conflict was correctly resolved in favour of consistency with the accepted in-codebase pattern. Flagging only for transparency; no change recommended.
