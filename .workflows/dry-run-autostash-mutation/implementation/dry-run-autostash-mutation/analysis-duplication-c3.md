AGENT: duplication
FINDINGS:
- FINDING: Two new autostash seed helpers duplicate the full read-gate timeline instead of composing
  SEVERITY: medium
  FILES: internal/engine/release_autostash_test.go:393-412 (seedAutostashHappyGit), internal/engine/release_autostash_test.go:417-435 (seedAutostashThroughTag)
  DESCRIPTION: This work unit added two new test helpers that each spell out the full ~14-line FakeRunner read-gate-through-tag timeline inline. They are near-byte-identical: a line-by-line diff shows seedAutostashThroughTag is exactly seedAutostashHappyGit minus only the final `push --atomic origin HEAD {tag}` scripted call — every other one of the 14 lines (rev-parse --show-toplevel, symbolic-ref, tag --list, stashSaved(), fetch, status --porcelain, abbrev-ref, rev-parse --verify, rev-list count, ls-remote, rev-parse HEAD, add, commit, remote get-url, tag -a) is duplicated verbatim. They also each re-list the same timeline the pre-existing seedHappyGit (release_test.go:49-66) already defines, differing only by the inserted stashSaved() slot after `tag --list`. The package's established convention is the opposite: the seedHappyGitThroughGate -> seedHappyGitThroughCommit -> seedHappyGitThroughTag chain (release_test.go:814-850) composes the timeline incrementally, each stage calling the previous and appending only its own SeedSequence delta. The two new helpers diverged from that convention and copy-pasted instead, so the canonical read-gate order now lives in yet two more independent copies that must be kept in sync as the spine's git call order evolves.
  RECOMMENDATION: Express seedAutostashThroughTag as seedAutostashHappyGit minus the push, following the existing -ThroughGate/-ThroughCommit/-ThroughTag composition pattern: have one helper script the timeline through the annotated tag and the other extend it (or factor the shared prefix into one helper both call, then append the push in the happy variant). This removes the ~14-line verbatim duplication between the two new helpers and keeps a single authored copy of the autostash read-gate order. (Folding the autostash helpers onto seedHappyGit itself — inserting a single stashSaved() slot — would also eliminate the duplication with the wider seed-helper family, but the broader seedHappyGit/seedAnyBranchHappyGit duplication is pre-existing and out of this work unit's scope.)
SUMMARY: The production change is clean — it threads a single skipCleanTree bool through runPreflight -> RunLocalGates mirroring the existing anyBranch parameter, with no duplicated logic. The only in-scope duplication is in the new test helpers: seedAutostashThroughTag re-lists the entire ~14-line timeline of seedAutostashHappyGit (identical bar the trailing push), bypassing the package's established incremental-composition convention for staged seed helpers.
