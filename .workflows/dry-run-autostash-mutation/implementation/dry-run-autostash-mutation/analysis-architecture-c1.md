AGENT: architecture
FINDINGS:
- FINDING: skipCleanTree boolean threading mirrors the established anyBranch seam — appropriate for the quick-fix scope
  SEVERITY: low
  FILES: internal/preflight/preflight.go:81, internal/engine/release.go:413, internal/engine/release.go:1106
  DESCRIPTION: code-quality.md lists "boolean parameters" and "long parameter lists (4+)" as anti-patterns, and the change pushes both RunLocalGates and runPreflight to two trailing booleans (anyBranch, skipCleanTree). This is worth NAMING so it is a conscious decision rather than an oversight — but on balance it is the RIGHT call here, not a defect to fix. The skipCleanTree boolean is a deliberate, symmetric mirror of the pre-existing anyBranch gate-skip: same call shape, same doc structure, same "skipped entirely / not evaluated" semantics, tested identically (TestRunLocalGates_SkipCleanTree_* parallels TestRunLocalGates_AnyBranch_*). RunLocalGates is a plain ordered driver, not a constructor, so the skill's functional-options guidance does not apply; introducing a gateOptions struct or option funcs for exactly two flags on a single internal call site would be over-engineering a quick-fix and would make this skip GRATUITOUSLY DIFFERENT from anyBranch right beside it — a worse seam than the consistent boolean. Consistency with the immediately-adjacent existing parameter is the dominant architectural value at this scope. No restructure recommended; flagged only so the boolean count is an accepted trade-off on record.
  RECOMMENDATION: Keep as-is. Do not refactor to an options struct for this quick-fix — that would break symmetry with anyBranch for no benefit. If a THIRD local-gate skip flag is ever added, that is the point to revisit collapsing the skip flags into a single typed value (e.g. a gateSkips struct shared by RunLocalGates and runPreflight), bringing anyBranch along so the seam stays uniform.
SUMMARY: The dry-run autostash gating and the skipCleanTree parameter threading are architecturally sound for a quick-fix: the new boolean is a deliberate, well-tested mirror of the existing anyBranch gate-skip, the bypass is correctly scoped to DryRun && AutoStash at a single call site, and the as-built WHY-comments are kept honest across release.go, autostash.go, and preflight.go. The only observation is that two trailing booleans now sit on RunLocalGates/runPreflight — an accepted trade-off for consistency, not a defect; an options struct would over-engineer this scope and diverge from the adjacent anyBranch convention.
