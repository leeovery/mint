AGENT: duplication
FINDINGS:
- FINDING: ~16 near-duplicate `seed*` FakeRunner scripters re-derive the same git call-order vocabulary
  SEVERITY: medium
  FILES: internal/commit/run_test.go:39 (seedDiffThenCommit), internal/commit/run_push_test.go:25 (seedDiffThenCommitThenPush), internal/commit/run_push_fail_test.go:41 (seedDiffThenCommitThenFailedPush), internal/commit/run_oversized_test.go:70,85 (seedOversizedFallback, seedPassesToL2), internal/commit/run_noai_test.go:45 (seedNoAIDefault), internal/commit/run_aifail_test.go:50 (seedAIFailFallback), internal/commit/run_edit_test.go:28 (seedEditThenAccept), internal/commit/run_edit_nolaunch_test.go:20,37 (seedEditNoEditorThenAccept, seedEditMissingBinaryThenAccept), internal/commit/run_regen_test.go:23 (seedRegenThenAccept), internal/commit/run_regen_fallback_test.go:67 (seedRegenFailFallback), internal/commit/run_editor_push_test.go:37 (seedNoAIDefaultThenPush), internal/commit/staging_defer_test.go:35,50 (seedAllModeThenStageAndCommit, seedAddAllModeThenStageAndCommit), internal/commit/run_failloud_test.go:64,75 (seedPreflightOnly, seedAIPreflightOnly)
  DESCRIPTION: Each of these per-file helpers independently builds `runner.NewFakeRunner()` + `SeedSequence("git", ...)` from the SAME hand-spelled `runner.ScriptedCall` vocabulary, in the SAME orchestrator-dictated order, with the SAME inline comments. The recurring tokens are copy-pasted verbatim across files: the non-empty empty-index preflight read `{Result: runner.Result{Stdout: "x\n"}} // git diff --cached --name-only` appears 43 times; the editor resolution `{Result: runner.Result{Stdout: editor + "\n"}} // git var GIT_EDITOR` 59 times; the bare commit sink `{} // git commit -F -` 44 times; plus shared L1-diff, ls-files-empty, add, and push calls. The helpers differ only in which subset of these calls they string together (e.g. seedDiffThenCommit = preflight+L1+commit; seedDiffThenCommitThenPush adds push; seedOversizedFallback swaps in the editor resolution; the staging_defer pair add an `add` call). This is classic copy-paste-then-tweak across task boundaries: the executors for the push, no-ai, ai-fail, edit, regen, oversized, and staging tasks each re-spelled the same skeleton. The drift risk is real — these sequences encode the production call ORDER in run.go (preflight read -> L1 diff -> [editor resolve] -> [stage] -> commit -> [push]); if that order changes, all ~16 must be hand-updated in lockstep, and a missed one silently mis-scripts a FakeRunner rather than failing obviously. (Note: this is distinct from the well-factored `editorDeps`/`editorDepsOptions` Deps builders and the `gitInvocationsOf`/`gitVerbInvocations` filters, which ARE already centralized — only the runner-scripting half escaped consolidation.)
  RECOMMENDATION: Extract a single named-step scripting builder into the existing shared test file (run_test.go, alongside `seedDiffThenCommit`). Give the recurring `ScriptedCall`s named constructors — e.g. `scPreflightNonEmpty()`, `scL1Diff(diff)`, `scEditorResolve(editor)`, `scLsFilesEmpty()`, `scAdd()`, `scCommit()`, `scPush()` / `scPushFail(stderr)` — and let each `seed*` helper compose its sequence from those (`r.SeedSequence("git", scPreflightNonEmpty(), scL1Diff(diff), scCommit(), scPush())`). This keeps each scenario's distinct call shape visible (the legitimate per-test variation) while spelling each git step, its result, and the call-order knowledge ONCE. The repeated `{Stdout: "x\n"}` preflight literal and the `editor + "\n"` resolution literal in particular should never be hand-typed in 40+ places. Do not collapse the helpers into one mega-builder with flags (that just trades duplication for a long parameter list); the goal is a shared step vocabulary, not a single God-helper.
SUMMARY: The commit PRODUCTION code is exemplary on duplication — shared helpers already exist for every cross-file pattern (sourceArgs/excludePathspecs/sourcesForMode for L1+preflight, commitAccept/pushAfterCommit/stageForMode for the accept tail, injectContext for both prompt knobs, isEmptyEditorBuffer/OpenEditor for both editor consumers). The one genuine, actionable duplication is in the test harness: ~16 `seed*` FakeRunner helpers across ~13 files each re-spell the same git scripted-call vocabulary and call order by hand, with the preflight/editor-resolve/commit literals copy-pasted 40-60 times each. Extract a shared named-step scripting vocabulary so the orchestrator's git call order lives in one place.
