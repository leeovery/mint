AGENT: standards
FINDINGS:
- FINDING: Oversized fallback under -y / non-TTY emits a misleading "opening editor" note immediately before failing loud
  SEVERITY: low
  FILES: internal/commit/run.go:286-289, internal/commit/run.go:392-394
  DESCRIPTION: On the oversized-diff path Run emits the case-3 note `p.Warn("diff too large to summarise — opening editor")` (run.go:287) and THEN calls runEditorFallback, whose no-message-source guard (run.go:392) fails loud with "no AI message and no interactive editor available" when deps.Yes is true or stdin is non-interactive. The user therefore sees a note promising "opening editor" followed immediately by a fail-loud saying no editor is available — no editor is ever opened. The spec (Commit Message Format & Prompt → The $EDITOR fallback, case 3; and the detection-ordering paragraph) describes the note as the clear signal when routing TO the editor, and the "-y/non-TTY forbidden-combo check (below) then applies"; it does not anticipate the note firing on a run that will never reach the editor. The AI-failure trigger (run.go:296-298) correctly carries NO note, so only the oversized trigger has this wrinkle. The fail-loud tests (run_failloud_test.go assertFailLoudNoMutation) assert the StageFailed message and no-mutation but do not assert the absence of the contradictory oversized Warn, so the inconsistent sequence is unverified. Spec-conformant on the substance (it fails loud, mutates nothing, surfaces the exact message); only the emitted-then-contradicted note is a UX drift from the spec's intent that the note announces an editor that is actually opening.
  RECOMMENDATION: Run the no-message-source guard (the deps.Yes || !deps.StdinInteractive check) for the oversized branch BEFORE emitting the oversized note — e.g. only emit the note when the fallback will actually open an editor, or move the unattended guard ahead of the Warn so an unattended oversized run fails loud with the single spec fail-loud message and no preceding "opening editor" claim. Add a fail-loud test asserting no oversized Warn is recorded on the -y / non-TTY oversized path.

- FINDING: Internal sentinel error strings carry punctuation (semicolons/commas), deviating from the lowercase-no-punctuation convention
  SEVERITY: low
  FILES: internal/commit/run.go:93, internal/commit/run.go:116, internal/commit/run.go:140, internal/commit/run.go:85
  DESCRIPTION: golang-naming and golang-error-handling both require error strings to be lowercase with no trailing punctuation; the broader idiom also avoids embedded sentence punctuation in error values. Several commit sentinels use comma/semicolon/colon punctuation: errEditorNoOp "commit aborted: editor closed with no message" (run.go:93), errPushFailed "commit: push failed; commit kept in place" (run.go:116, semicolon), errRegenerateFallback "commit: regenerate failed, routing to editor fallback" (run.go:140, comma). Per their own doc comments errPushFailed and errRegenerateFallback are INTERNAL-only routing sentinels that are never surfaced to the user (they carry no user-facing message and only drive the exit code / fallback routing), so the impact is confined to source readability rather than user output. errGateAborted (run.go:85) and errEditorNoOp are also non-narrated no-op causes. This is a minor convention deviation, not a behavioural drift. Note: the user-facing errNoMessageSource intentionally keeps "AI" capitalised because the spec pins that exact string verbatim ("no AI message and no interactive editor available") — that is a correct spec-wins decision, not a finding.
  RECOMMENDATION: Optionally simplify the internal sentinel strings to comma/semicolon-free lowercase phrases (e.g. "regenerate failed routing to editor fallback", "push failed commit kept in place") to align with the error-string convention. Low priority given these are never user-surfaced; leave errNoMessageSource exactly as-is (spec-pinned).
SUMMARY: The commit-command implementation is in strong conformance with the specification across all sections — exact empty-staging/fail-loud/oversized message strings, the y/n/e/r gate contract with loop-back vs save-as-accept correctly separated, the three converging "no AI message" fallback triggers, deferred-staging mutate-nothing-until-accept, warn-don't-unwind push, the deliberately dropped preflight gates, the verb-namespaced [commit] config, and the exact CLI flag surface (no -m, no --dry-run, no --context). Only two low-severity items surfaced: a misleading "opening editor" note emitted just before an unattended oversized run fails loud, and minor punctuation in internal-only sentinel strings. vet, golangci-lint, and go test all pass clean.
