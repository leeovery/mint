AGENT: duplication
FINDINGS:
- FINDING: Test invocation-filter helpers duplicated 9× across the commit test suite
  SEVERITY: medium
  FILES: internal/commit/run_test.go:73 (gitInvocations), internal/commit/run_test.go:743 (commitInvocations), internal/commit/run_push_test.go:17 (pushInvocations), internal/commit/staging_defer_test.go:16 (addInvocations), internal/commit/run_noai_test.go:37 (editorGitInvocations), internal/commit/run_noai_test.go:47 (editorAddInvocations), internal/commit/run_noai_test.go:57 (editorCommitInvocations), internal/commit/run_editor_push_test.go:17 (editorPushInvocations), internal/commit/generate_test.go:96 (gitInvocationsGen)
  DESCRIPTION: Nine helper functions implement the same two filtering shapes over a recorded invocation slice. (a) "keep only Name==git" appears verbatim 3× — gitInvocations, gitInvocationsGen, editorGitInvocations are byte-for-byte the same loop, differing only in whether they read r.Invocations() or er.fake.Invocations(). (b) "keep only git calls whose Args[0]==<verb>" appears 6× — addInvocations, editorAddInvocations, commitInvocations, editorCommitInvocations, pushInvocations, editorPushInvocations — each a copy-pasted 6-line loop differing only by the verb literal ("add"/"commit"/"push") and the source (raw runner vs editorRunner.fake). No shared filter exists in internal/runner (FakeRunner.Invocations is the only accessor), so each test file independently re-rolled the same loop. This is the classic copy-paste-drift surface: a future change to how invocations are matched (e.g. recording flag-merged args) must be made in nine places.
  RECOMMENDATION: Add two shared helpers in the commit test package (e.g. run_test.go or a small testutil file): gitInvocationsOf(invs []runner.Invocation) []runner.Invocation and gitVerbInvocations(invs []runner.Invocation, verb string) []runner.Invocation, both taking an already-extracted []runner.Invocation so the same helper serves both the raw-runner and editorRunner.fake call sites. Rewrite the nine wrappers to one-line delegations (or delete them where the call site can call the shared helper directly). gitVerbInvocations subsumes all six verb filters; gitInvocationsOf subsumes the three Name==git copies.

- FINDING: Per-file commit.Deps builder functions are near-duplicate scaffolding (7 variants)
  SEVERITY: medium
  FILES: internal/commit/run_aifail_test.go:23 (aiFailDeps), internal/commit/run_oversized_test.go:102 (oversizedDeps), internal/commit/run_regen_fallback_test.go:56 (regenFailDeps), internal/commit/run_edit_test.go:19 (editDeps), internal/commit/run_noai_test.go:19 (noAIDeps), internal/commit/run_failloud_test.go:88 (failLoudDeps), internal/commit/run_test.go:61 (newCommitDeps)
  DESCRIPTION: Each editor-path test file authored its own commit.Deps constructor and they have converged on the same body. aiFailDeps, oversizedDeps and regenFailDeps are IDENTICAL — same signature (rec, er, tr, mode, root), same struct literal: Presenter:rec, Runner:er, Committer:git.NewMutator(er, git.WithBackoff(func(int){})), Transport:tr, Root:root, Staging:mode, StdinInteractive:true. editDeps and noAIDeps are subset variants of the same shape (editDeps drops mode, noAIDeps drops Transport and sets NoAI:true). failLoudDeps is the superset — it already exposes every varying field (mode, root, yes, stdinInteractive, noAI, tr). Because each was written in isolation, the shared git.NewMutator(er, git.WithBackoff(func(int){})) wiring and the StdinInteractive:true default are copy-pasted six times; a change to the editorRunner Committer wiring (e.g. backoff signature) touches every file.
  RECOMMENDATION: Keep one editor-path Deps builder — failLoudDeps is already the superset and can be promoted to a shared editorDeps(rec, er, tr, mode, root, yes, stdinInteractive, noAI) (or an options-struct variant to avoid the boolean-triple parameter list). Have aiFailDeps/oversizedDeps/regenFailDeps/editDeps/noAIDeps either be deleted in favour of direct calls with the TTY defaults, or become thin one-line wrappers that fix the per-scenario fields (e.g. noAI=true for the --no-ai path). The newCommitDeps bare-path builder (FakeRunner + no editor) is a genuinely different shape and may stay separate.

- FINDING: Accept-path tail (stage → commit → push → RunFinished) duplicated between the two accept entry points
  SEVERITY: low
  FILES: internal/commit/run.go:333 (Run, gate-accept tail), internal/commit/run.go:422 (runEditorFallback, save-as-accept tail)
  DESCRIPTION: The two accept paths end with the same ordered sequence: stageForMode → (surface on err) → createCommit → (surface on err) → pushAfterCommit → p.RunFinished(...) → return pushErr. Run lines 333-349 and runEditorFallback lines 422-438 are the same five-step tail with the same error-surfacing and the same "RunFinished always fires, return pushErr" contract; only the committed body differs (finalBody vs saved). The individual steps are already well-extracted (stageForMode, createCommit, pushAfterCommit), so the remaining repetition is just the orchestration glue — but it is exactly the spec's load-bearing "stage→commit→push, mutate-nothing-until-accept, never-unwind" invariant, and having it written twice means a future change to that ordering (or to RunFinished firing) must be kept in sync across both accept paths.
  RECOMMENDATION: Extract a single commitAccept(ctx, deps, root, body string) error helper that runs stageForMode → createCommit → pushAfterCommit → RunFinished and returns pushErr, and call it from both the gate-accept branch and the save-as-accept branch. This collapses the duplicated tail to one call site each and makes the stage→commit→push ordering invariant single-sourced. Proportionality note: this is ~8 lines each, borderline on Rule-of-Three — flagged low because it encodes a cross-cutting safety invariant the spec stresses, not because of raw line count.
SUMMARY: The production code is well-factored — its three "no AI message" fallbacks already converge on shared helpers (OpenEditor, runEditorFallback, isEmptyEditorBuffer, pushAfterCommit) and the intentional mirroring of release/notes (ResolveCommitPrompt, the prompt composer, excludePathspecs) is correctly NOT duplication. The duplication that did emerge is concentrated in the test suite, where isolated per-file authoring produced ~9 copy-pasted invocation-filter helpers and ~6 near-identical Deps builders; one minor production extraction (the twin accept-path tails) is also worth consolidating because it single-sources a spec invariant.
