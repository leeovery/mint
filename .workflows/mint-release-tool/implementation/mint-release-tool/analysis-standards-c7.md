AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to specification and project conventions. Cycle-6 fixes verified clean: the AI transport real-deadline test (internal/ai/transport_test.go) is now deterministic (no flaky invocation-count assertion under load; classification-only assertion against a real deadline kill), and the cmd-layer regenerate producers are fully consolidated (reuse/fresh dispatch in exactly one place; single-version delegates to the canonical batch producers; both call sites wired). No new drift across the load-bearing invariants — PONR asymmetry + pre-PONR ctx.Err() gate, surgical unwind on tracked MadeState with context.WithoutCancel recovery survival + manual-cleanup warns, autostash unwind-then-pop ordering, regenerate write-path recovery, preflight subset-per-verb + axis-contract validation, lock-resilient Mutator (stale-vs-live, fresh-reader-per-retry), exec_runner ctx.Err()-before-ExitError timeout classification, editor $VISUAL→$EDITOR→vi resolution. Tests deterministic throughout (no time.Sleep/After/Tick in changed test files; injected clocks; t.Parallel; exact-output assertions).
