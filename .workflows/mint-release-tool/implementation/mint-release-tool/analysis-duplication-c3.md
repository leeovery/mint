AGENT: duplication
FINDINGS:
- FINDING: Release-bookkeeping commit subject `{prefix} Release {tag}` built independently in three places
  SEVERITY: high
  FILES: internal/record/commit.go:80, internal/release/release.go:125, internal/engine/release.go:1258
  DESCRIPTION: The exact subject `fmt.Sprintf("%s Release %s", commitPrefix, tag)` is constructed from scratch in three units that all describe the SAME release commit: record.CommitBookkeeping makes the actual bookkeeping commit with it (commit.go:80), release.composeTagMessage embeds it as the annotated-tag subject line (release.go:125), and engine.bookkeepingSubject (release.go:1258) rebuilds it for the --dry-run plan. The engine helper's own comment ("It mirrors record.CommitBookkeeping's own subject format") concedes the coupling. These three MUST stay byte-identical — the dry-run plan promises the exact subject a real run will commit, and the tag annotation subject is expected to match the bookkeeping commit subject — yet each owns its own literal, so a format change (a colon, casing, spacing) in one silently desyncs the plan/tag from the real commit.
  RECOMMENDATION: Extract a single exported subject builder — e.g. record.BookkeepingSubject(commitPrefix, tag) string alongside the existing record.ChangelogFileName — and have all three call sites use it (CommitBookkeeping internally, release.composeTagMessage, engine.bookkeepingSubject delegate, dropping the local fmt.Sprintf). One format, one place to change.

- FINDING: CHANGELOG stage-and-commit idiom duplicated between single-version and batch regenerate paths
  SEVERITY: medium
  FILES: internal/engine/regenerate_changelog.go:68-74, internal/engine/regenerate_batch_changelog.go:148-153
  DESCRIPTION: Both regenerate paths perform the same two-step git mutation block: `m.Mutate(ctx, nil, "git", "-C", root, "add", record.ChangelogFileName)` then `m.Mutate(ctx, nil, "git", "-C", root, "commit", "-m", subject)`, each with a parallel "staging %s" / "committing %q" error-wrap pair. The ONLY difference is the commit subject (single-version vs batch). The shared push/recovery tail was already factored into pushChangelogCommit (task 8-1), but the stage+commit half was left duplicated. A change to how the changelog is staged/committed must currently be made in both blocks or they drift.
  RECOMMENDATION: Extract an engine helper — e.g. stageAndCommitChangelog(ctx, m, root, subject) error — owning the git add CHANGELOG.md + git commit -m subject sequence and its error wrapping, parameterised only by subject. RegenerateChangelog and commitAndPushRebuild then each pass their own subject, mirroring how pushChangelogCommit already consolidates the shared push tail.

- FINDING: Publisher-resolution composition repeated verbatim across the two cmd regenerate entry points
  SEVERITY: low
  FILES: cmd/mint/main.go:166, cmd/mint/regenerate_all.go:49
  DESCRIPTION: Both regenerate entry points contain the byte-identical line `publisher, _ := publish.ResolvePublisher(engine.RemoteURL(ctx, r), cfg.Release.Provider, r)` preceded by a near-identical "non-github / no-remote host downgrades..." comment. The engine already exports RemoteURL to share the "empty == unresolved" reader, and the engine's own resolvePublisher (release.go:893) composes the same three arguments — so the ResolvePublisher(RemoteURL(...), provider, runner) composition now exists in three spots, two an exact cmd-layer copy.
  RECOMMENDATION: Add one exported engine helper wrapping the composition — e.g. engine.ResolvePublisher(ctx, r, cfg) (publish.Publisher, error) reusing the existing RemoteURL — and have both cmd regenerate functions (and where it fits, the forward resolvePublisher) call it.
SUMMARY: Three consolidation candidates from independently-authored tasks: the release-commit subject format is rebuilt in three units that must stay identical (high), the CHANGELOG stage+commit block is duplicated across single-version and batch regenerate (medium), and the publisher-resolution composition is copied across both cmd regenerate entry points (low). Atomic-write (fsutil.WriteFile), stage narration, hook-env, and the regenerate push/recovery tail (pushChangelogCommit) are already properly consolidated.
