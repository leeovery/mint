AGENT: architecture
FINDINGS:
- FINDING: Release-success footer URL is a built-but-never-fed presenter capability
  SEVERITY: low
  FILES: internal/engine/release.go:624, internal/engine/release.go:646, internal/publish/publish.go:48-100, internal/presenter/pretty.go:925-929, internal/presenter/plain.go:482-486
  DESCRIPTION: The presenter exposes a RunResult.URL field with full rendering in both modes — pretty renders `{leaf} released {project} v{X} · {url}` and plain renders `done: {project} v{X} {url}` — but the forward release path hardcodes `releaseURL := ""` and never populates it. Publisher.CreateRelease returns only error; GitHubPublisher discards the runner Result (`_, err := p.runner.RunWith(...)`), throwing away the release URL that `gh release create` prints to stdout. So the entire `· {url}` / `done: ... {url}` rendering branch is dead in production: every real release closes with an empty URL segment, even though the seam was plumbed end-to-end (field, both renderers, tests). The Publisher boundary cannot carry the URL back, so the presenter capability behind it can never light up. Whether the footer URL is wanted is a product call; as built it is latent surface that no production path can reach.
  RECOMMENDATION: Either (a) close the seam — change CreateRelease (and UpdateRelease) to return the created/updated release URL (it is on the gh stdout the driver already discards) and thread it into RunResult.URL at release.go:624/646 so the rendered footer is fed; or (b) if a URL footer is out of scope, drop the URL field and its two render branches so the presenter contract does not advertise a capability the engine cannot supply. Do not leave the field plumbed-but-starved.
- FINDING: Orphaned Phase-1 presenter mappers (EmitPlan/EmitStageFailed/EmitNotes/EmitWarning) survive only to be tested
  SEVERITY: low
  FILES: internal/engine/engine.go:123-154, internal/engine/engine_test.go:153-212
  DESCRIPTION: EmitPlan, EmitStageFailed, EmitNotes, and EmitWarning are exported thin event->method mappers whose own doc comments call them "thin Phase 1 event->method mappers proving the adoption is real." The real orchestrator (release.go) calls p.ShowPlan / p.StageFailed / p.ShowNotes / p.Warn directly and never routes through these helpers. Confirmed zero production callers — the only references are in engine_test.go. They are leftover walking-skeleton scaffolding the actual spine superseded, now exported parallel indirection over the presenter that exists purely so its tests can exercise it. (ReviewDecision and FirstReleaseReviewGate in the same file ARE live — scope is the four Emit* functions only.) Dead exported API surface that invites a future caller to use a deprecated path and adds noise to the engine's public boundary.
  RECOMMENDATION: Remove the four Emit* functions and their dedicated tests, since the orchestrator already calls the presenter methods directly. If any is genuinely wanted as a named seam, wire it at its single call site in release.go rather than leaving it unreferenced.
SUMMARY: The implementation composes well overall — clean seams (CommandRunner/Mutator/Publisher/Presenter), correct forward-vs-regenerate differentiation, properly gated dry-run cache write, and no production stubs or half-wired commands. Two low-severity surface issues: a release-success URL plumbed through the presenter but never fed because the Publisher seam stops one layer short, and four orphaned Phase-1 Emit* mappers that only their own tests call.
