AGENT: architecture
FINDINGS:
- FINDING: AI transport production-default construction duplicated across forward and regenerate paths
  SEVERITY: low
  FILES: internal/engine/release.go:835-840, internal/engine/regenerate_fresh.go:126-131
  DESCRIPTION: aiTransport (forward) and resolveFreshTransport (regenerate) are byte-for-byte the same resolution policy: "use the injected notes.Transport when non-nil, else build the production transport as ai.NewTransport(r, ai.Config{AICommand: cfg.AICommand})". Both the injection-precedence rule and the load-bearing production-default literal (the AICommand wiring) are authored independently in two functions in the same package. The seam is wired correctly today and each is documented as mirroring the other, but the spec treats the regenerate fresh path as "the forward Stage-4 AI path with ONE substitution: the diff base". If the default transport construction ever changes (a new ai.Config field, a different default command, transport-level options), both call sites must be updated in lock-step or forward and regenerate AI behaviour silently drifts. Below the Rule-of-Three threshold (two instances), so a low-severity composition note, not a defect.
  RECOMMENDATION: Derive both from one helper — e.g. resolveAITransport(r runner.CommandRunner, injected notes.Transport, cfg config.Config) notes.Transport that aiTransport (passing deps.Runner, deps.Transport) and freshGenerator (passing r, transport) both call. The production-default literal then exists once, so forward and regenerate AI transport can no longer drift.
SUMMARY: Architecture is sound — the lock-resilient git Mutator is constructed once and threaded through every mutation seam; the shared pushChangelogCommit tail, the consolidated ExcludeConfig domain type, the surgical Unwind driven off tracked MadeState, and the DispatchRelease (regenerate create-or-update) vs direct CreateRelease (forward) divergence all compose cleanly and match the spec. The dead Emit* mappers were correctly removed last cycle. The only seam concern is a low-severity duplicated AI-transport default-construction literal between forward and regenerate paths.
