AGENT: standards
FINDINGS:
- FINDING: AI-output validation omits the spec-listed "refusal" check
  SEVERITY: low
  FILES: internal/ai/transport.go:177-179 (isValid), internal/ai/transport.go:170-176 (doc rationale)
  DESCRIPTION: The spec's "Output format & validation" section (specification.md:302) defines AI-output validation as "sanity, not structure: non-empty, not an error/refusal/whitespace." The implementation's isValid checks only non-empty / non-whitespace; the "error" case is covered separately (a non-zero command exit is treated as bad content and retried), but there is NO refusal detection of any kind (grep finds no refusal/cannot/sorry/unable heuristic anywhere in internal/ai or internal/notes). The transport's own doc comment makes this an explicit, reasoned choice ("refusal detection is deliberately MINIMAL ... the human review gate is the backstop for style"). This is defensible: a non-interactive `-y` run is the only path with no human backstop, and the spec frames prompt wording as ship-and-refine. So this reads as a deliberate, documented interpretation rather than an oversight — hence low severity — but the spec does name "refusal" in the validation checklist, so the gap is genuine and worth recording.
  RECOMMENDATION: Confirm the team intends to drop the spec's "refusal" validation item (relying on the review gate / one retry), and if so leave a one-line note at isValid pointing back at the spec decision so the divergence is traceable. If a minimal refusal guard IS wanted, add a small case-insensitive prefix/marker check (e.g. a leading "I can't"/"I'm sorry"/"I cannot") that routes to the existing single-retry-then-ErrNotesFailure path — sanity-level, not structure-level, exactly as the spec frames it.
SUMMARY: The cycle-1 finding (regenerate fresh path bypassing the degenerate guard) is fully resolved — the IsDegenerate short-circuit now lives in the shared generateFromDiffWithContext core, so forward, single-version fresh, and --all batch fresh all inherit it. A fresh independent pass over every other major spec decision point found everything conforming to the spec and Go conventions. The single recorded item is one low-severity, deliberately-documented omission of the spec-listed "refusal" validation check.
