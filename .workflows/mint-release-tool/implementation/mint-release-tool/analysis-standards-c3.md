AGENT: standards
FINDINGS:
SUMMARY: STATUS clean — the mint release tool implementation conforms to the specification and Go project conventions; no material spec-conformance gaps or convention violations found. Verified every pipeline decision point against the specification (Stage 1 version/tag grammar + --set-version gate, Stage 2 preflight gate set + escape hatches, hooks mechanism + MINT_* env + pre_tag interplay, Stage 4 AI notes two-tier exclusion + Change Map + notes-path precedence + max_diff_lines + retry/timeout + KaC emoji skin, body distribution + Stage 5 record idempotent replace + version-file plain/embedded fail-loud, PONR asymmetry + surgical unwind off tracked MadeState + lock-resilient Mutator + safe downgrade + dry-run cache, regenerate two-axis contract + version normalisation + reuse no-body handling + batch skip-and-continue + whole-file rebuild + per-verb preflight subset, config schema strict validation, editor resolution). go build / go vet / golangci-lint (0 issues) / go test ./... (all 20 packages) all pass. Error strings lowercase+unpunctuated, %w wrapping with errors.Is/As, accept-interfaces seams, no swallowed errors, single-handling rule holds.
