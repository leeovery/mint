AGENT: duplication
FINDINGS:
- FINDING: cmd-layer regenerateTarget repeats the raw "changelog || both" predicate instead of a shared method
  SEVERITY: low
  FILES: cmd/mint/regenerate_validate.go:35, cmd/mint/regenerate_validate.go:62, cmd/mint/regenerate_run.go:43-50
  DESCRIPTION: The cmd-layer regenerateTarget enum has no predicate methods, so "is this a changelog-touching target?" is open-coded as the literal `target == targetChangelog || target == targetBoth` in validateRegenerateRequest (line 35) and again in validateTargetAgainstChangelog (line 62). The engine-side sibling RegenerateTarget exposes this exact concept once as the named writesChangelog()/writesProvider() methods (regenerate_write.go:91,96), consumed cleanly in six engine call sites. The cmd layer reimplements the same boolean inline in two places; a future fourth target value would have to be added to every open-coded disjunction, and the two layers can drift on what "writes the changelog" means.
  RECOMMENDATION: Add a writesChangelog() method on the cmd-layer regenerateTarget mirroring the engine's, and replace the two `targetChangelog || targetBoth` disjunctions in regenerate_validate.go with the method call. Keeps the two enums separate (concrete types at different boundaries by design) while giving each layer one authoritative predicate. Low severity: only two inline sites and the engine already owns the canonical version.
- FINDING: regenerate provider warn helpers parallel the forward publish warn helpers with the same Warn-seam shape
  SEVERITY: low
  FILES: internal/engine/regenerate_write.go:381-399, internal/engine/release.go:1490-1518
  DESCRIPTION: warnRegenerateProviderSkipped/warnRegenerateProviderFailed and warnPublishDowngraded/warnPublishFailed are four near-identical helpers: each builds a presenter.Warning with a Label, a fixed Message, and (for the *Failed pair) cause.Error() in Output. The bodies differ only in literal message wording; the doc comments explicitly note the mirroring.
  RECOMMENDATION: Keep the distinct user-facing wording (forward heals with `regenerate --reuse`, regenerate heals with `--target release`), but optionally extract a tiny internal warn(p, label, message, cause) helper the four sites pass literals to, removing the repeated Warning-struct construction without merging wording. Arguably below the Rule-of-Three bar; acceptable to leave as-is.
SUMMARY: The Phase 10 remediation already consolidated the major cross-file duplication aggressively and explicitly — single-version and batch regenerate paths share stageAndCommitChangelog, pushChangelogCommit, resetAndAbort, readHistoricalDate, and resolveHEAD; publisher resolution is unified behind engine.ResolvePublisher/RemoteURL; the exec runner shares translateRun; and the deleted cmd-layer regenerateGateSet mapper correctly collapsed onto the engine-side regenerateGateSet with no residual cmd-layer reimplementation. Only two low-severity residues remain.
