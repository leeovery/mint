AGENT: duplication
FINDINGS: none
SUMMARY: No significant duplication detected across implementation files. Production code routes shared logic through deliberate single-point helpers (writeNotesBody, readChoice/parseChoice, bufferedReader, formatElapsed, leafOrDefault, padStage, IsTerminal), and both presenters' renderReleaseFooter and -y echo paths derive from the same gate payload. The test suite is equally consolidated: construction-and-capture (drive/drivePretty/prettyGate/plainGate/gateDriver/drivePrettySpy/drivePrettyPromptProfile), dependency guards (assertImportsExclude), byte-purity (assertBytePureASCII), ANSI handling (stripANSI), rule fixtures (notesTitledRule), and test doubles (spyTracker, failingReader) are each defined once and reused. The only repeated idiom — inline NewPrettyPresenter(out, WithProfile(...)) construction across a few pretty-side test files — is a two-line construct with legitimate per-call option variation, below the Rule-of-Three multi-line-block bar; extracting it is not warranted. CLEAN.
