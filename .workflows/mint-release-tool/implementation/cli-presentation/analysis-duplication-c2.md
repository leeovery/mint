AGENT: duplication
FINDINGS:
- FINDING: Presenter-construction-and-capture boilerplate copy-pasted across gate test files
  SEVERITY: medium
  FILES: internal/presenter/gate_skip_test.go:34-37, internal/presenter/gate_skip_test.go:58-59, internal/presenter/gate_skip_test.go:77-79, internal/presenter/gate_skip_test.go:94-96, internal/presenter/gate_forbidden_test.go:27-29, internal/presenter/gate_forbidden_test.go:49-50, internal/presenter/gate_forbidden_test.go:66-68, internal/presenter/gate_forbidden_test.go:82-84, internal/presenter/gate_forbidden_test.go:109-110, internal/presenter/gate_sourcetarget_test.go:84-85, internal/presenter/gate_sourcetarget_test.go:103-113
  DESCRIPTION: The three-or-four-line idiom `out := &bytes.Buffer{}; errBuf := &bytes.Buffer{}; p := presenter.NewPlainPresenterWithInput(out, errBuf, strings.NewReader(input)).WithYes(...)` (and its `NewPrettyPresenter(out, WithProfile(...), WithInput(...))` pretty twin) is hand-inlined dozens of times across gate_skip_test.go, gate_forbidden_test.go, and gate_sourcetarget_test.go. plain_test.go and pretty_test.go already established the `drive`/`drivePretty` capture helpers and prompt_test.go established `drivePlainPrompt`/`drivePrettyPrompt`, but the gate-* files (written as separate task slices) did not reuse them and re-authored the construction inline. Because each call site repeats the option list (WithProfile, WithInput, WithYes, WithInteractiveStdin) by hand, a future construction-API change must be touched at ~30 sites instead of 2, and the `.WithYes(true)` / `.WithInteractiveStdin(false)` arming flag is easy to omit by copy-paste drift.
  RECOMMENDATION: Reuse / extend the existing prompt drivers. Add small shared constructors in a single test helper (e.g. a `gate_test_helpers.go` in package `presenter_test`) such as `plainGate(input string, opts...) (p, out, errBuf)` and `prettyGate(input string, profile, opts...) (p, out)` that take the `-y` / interactive-stdin toggles as parameters, then have gate_skip_test.go, gate_forbidden_test.go, and gate_sourcetarget_test.go call them. Do not invent new behaviour — only collapse the repeated construction+capture lines already present.
- FINDING: Plain/pretty parity test pairs duplicate the same assertion body twice inline
  SEVERITY: medium
  FILES: internal/presenter/prompt_test.go:39-57, internal/presenter/gate_skip_test.go:303-330, internal/presenter/gate_forbidden_test.go:126-285, internal/presenter/gate_sourcetarget_test.go:135-160
  DESCRIPTION: Many tests assert "the same outcome holds in BOTH render modes" by writing the plain block, then copy-pasting a structurally identical pretty block immediately below (drive plain, assert choice/error; drive pretty, assert choice/error). prompt_render_only_test.go solved exactly this with the `promptDrivers()` table + `t.Run(d.mode, ...)` pattern, but the surrounding gate test files predominantly hand-duplicate the two arms. The two arms drift independently — e.g. one arm gets an extra assertion the other lacks — and a new mode-invariant property must be written twice.
  RECOMMENDATION: Where a property is mode-invariant, drive it through the existing `promptDrivers()`-style table (one driver per mode) and assert once inside `t.Run(d.mode, ...)`. Keep mode-SPECIFIC rendering tests (exact bytes of the plain hint vs the pretty menu) separate — only consolidate the arms whose assertion body is genuinely identical across modes.
- FINDING: Decorative notes-rule construction re-implemented in two test files and re-derived from production
  SEVERITY: medium
  FILES: internal/presenter/pretty_test.go:1179-1191, internal/presenter/pretty_width_test.go:17-19, internal/presenter/pretty.go:656-670
  DESCRIPTION: pretty_test.go defines `notesTitledRule`/`notesClosingRule` (rebuilding the `"── release notes · v"+version+" "` prefix, the `width - len([]rune(prefix))` fill clamp, and the U+2500 repeat), while pretty_width_test.go independently defines `notesTitlePrefix` rebuilding the same literal prefix string. Both are parallel reconstructions of production's `notesTitledRule`/`notesClosingRule` in pretty.go, and the `"── release notes · v"` literal plus the fill/clamp arithmetic now live in three places. If the rule wording or fill math changes in production, two separate test reconstructions must be updated in lock-step or the tests silently encode the old format. The `decorativeRuleWidthForTest = 50` constant in pretty_test.go also re-encodes production's `ruleCap` (width.go:22).
  RECOMMENDATION: Consolidate the test-side rule expectation into one shared helper (the prefix string + the fill/clamp logic in a single place reused by both pretty_test.go and pretty_width_test.go), and reference the production `ruleCap`-equivalent value once rather than hardcoding `50` in a second spot. This removes the third copy of the prefix literal and the second copy of the fill arithmetic.
- FINDING: ANSI-stripping / display-width helper localised to one file but needed by colour-aware assertions elsewhere
  SEVERITY: low
  FILES: internal/presenter/pretty_width_test.go:33-47, internal/presenter/pretty_test.go:1296, internal/presenter/pretty_test.go:1315
  DESCRIPTION: `stripANSI` (a CSI-SGR stripper) lives in pretty_width_test.go and is used only there, yet pretty_test.go and the pretty gate tests assert on rendered output that may carry ANSI by pinning `termenv.Ascii` to dodge the problem rather than stripping. This is not yet a hard duplication, but it is a near-duplicate-in-waiting: the moment another pretty test needs to assert structure under a colour-capable profile it will either re-author a stripper or be forced onto the Ascii workaround. The helper is the natural shared primitive for "assert layout independent of colour."
  RECOMMENDATION: Promote `stripANSI` (and its `ruleDisplayWidth` consumer) into the shared pretty test-helper file alongside the rule helpers from the previous finding, so colour-aware assertions across pretty_test.go / pretty_width_test.go / pretty_gate_test.go draw on one stripper instead of spawning a second copy.
- FINDING: WithYes / WithInteractiveStdin builder setters duplicated verbatim across the two presenter structs
  SEVERITY: low
  FILES: internal/presenter/plain.go:99-114, internal/presenter/pretty.go:267-282
  DESCRIPTION: `WithYes` and `WithInteractiveStdin` are byte-for-byte parallel implementations on PlainPresenter and PrettyPresenter (same body, same multi-paragraph doc comment), as are the `yes`/`stdinInteractive`/`terminalFailure` fields and their long doc blocks. This is largely inherent to the spec's "two implementations behind the interface" mandate and Go's lack of struct-mixin inheritance, so it is NOT a high-impact extraction target — the receivers are distinct concrete types and the shared parse/loop core is already correctly factored. Flagged only for awareness.
  RECOMMENDATION: Leave the setters as-is (extracting across two concrete types would add an embedded-struct indirection that costs more clarity than the 3-line bodies save). If anything, deduplicate the near-identical doc comments by pointing one at the other rather than maintaining two full copies that can drift.
SUMMARY: Production-side duplication is minimal — the shared parse/loop core (readChoice/parseChoice/bufferedReader), writeNotesBody, and leafOrDefault are already factored, and the remaining plain/pretty parallelism is the spec-mandated two-implementation design. The meaningful duplication is on the test side: gate test files (written as separate slices) re-inline presenter-construction boilerplate and copy-paste both-mode assertion arms instead of reusing the existing drive/promptDrivers helpers, and the decorative notes-rule expectation is reconstructed in three places.
