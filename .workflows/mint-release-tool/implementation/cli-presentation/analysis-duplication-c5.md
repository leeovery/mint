AGENT: duplication
FINDINGS:
- FINDING: Import-scanning dependency-guard body duplicated across two test files
  SEVERITY: low
  FILES: internal/presenter/plain_test.go:1013-1029, internal/presenter/prompt_render_only_test.go:62-85
  DESCRIPTION: Two test files independently re-author the same AST import-scanning guard. `TestPlainPresenterImportsNoUILibrary` (plain_test.go) and `TestPromptPathImportsNoSubprocessDependency` (prompt_render_only_test.go) both run the identical core: `fset := token.NewFileSet()` → glob sources → `parser.ParseFile(fset, path, nil, parser.ImportsOnly)` (with a `t.Fatalf` on parse error) → range `file.Imports` → `strings.Trim(imp.Path.Value, "\"")` → range a marker list → match → `t.Errorf`. The prompt_render_only_test.go version's own doc comment states it "MIRRORS the UI-library guard ... and its go/parser ImportsOnly approach" — an explicit acknowledgement of the copy. The two diverge only in (a) which sources they scan (plain.go vs all non-test sources, each already its own named helper: `plainPresenterSources` / `presenterNonTestSources`), (b) the marker slice (`uiLibraryMarkers` vs `subprocessMarkers`), and (c) the match predicate (`strings.Contains` vs `==`). The ~12-line parse-and-scan loop is the duplicated part; the marker lists and source globs are legitimately distinct. Copy-paste across task boundaries (a Phase-1 plain guard and a Phase-3 render-only guard authored in isolation); a future change to the parse/scan mechanics must be made in two places.
  RECOMMENDATION: Extract a single shared test helper taking the source paths, a marker list, and a match mode — e.g. `assertImportsExclude(t, sources []string, markers []string, exact bool)` — running the parse-and-scan loop once, and have both guards call it with their own sources/markers. The existing per-guard source-glob helpers and marker slices stay as-is; only the duplicated scan body collapses. Keep the prompt guard's `scanned == 0` defence inside the shared helper so both guards gain the regression protection.
SUMMARY: The implementation is unusually well-factored against duplication — production code already shares writeNotesBody, formatElapsed, readChoice/parseChoice, bufferedReader, leafOrDefault, and the IsTerminal probe, and the tests share construction/capture seams. The only genuine cross-file duplication is the AST import-scanning guard body re-authored in two test files; the parallel plain/pretty two-arm test structure and the per-mode exact-byte -y echo assertions are intentional mode-specific assertions the codebase deliberately keeps separate, not accidental copies.
