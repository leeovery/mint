AGENT: standards
FINDINGS:
- FINDING: Pretty spinner / plain start-line text drops the engine's stage-specific narration the spec's worked example shows
  SEVERITY: low
  FILES: internal/presenter/pretty.go:364-373, internal/presenter/plain.go:152-157, internal/presenter/presenter.go:348-354
  DESCRIPTION: The spec's worked examples render the blocking start line as `⠋ notes  generating with claude…` (pretty) and `notes: generating…` (plain). The implementation instead renders only the bare stage name in pretty (`⠋ notes`) and a generic stage-agnostic `{name}: running...` in plain, because the StageStart payload carries only Name + Blocking — no start-detail field. This is a defensible resolution of the event-payload principle (the presenter must not hardcode stage-specific text, and the spec explicitly grants "wording refinable" latitude and calls the worked-example wording illustrative), so it does NOT violate the spec's explicit payload contract. It is flagged only because the chosen StageStart shape gives up an affordance the spec's examples imply. Not a drift to fix now — recorded for visibility. NOTE: this same observation was raised and resolved in analysis cycles 1 (task 5-2 changed the plain verb to the stage-agnostic "running...") and 2 (recorded-only); the event-payload principle has consistently been judged to correctly supersede the worked-example verb.
  RECOMMENDATION: No change required for spec conformance (consistent with cycle-1/cycle-2 resolution). If the spec's exact start-line wording is later deemed load-bearing, add an engine-supplied start-detail field to StageStart (preserving the payload principle) rather than hardcoding stage-name-keyed text in the presenter.
SUMMARY: The implementation is highly faithful to the specification — two-mode pretty/plain seam, TTY-only mode detection with no env sniffing, fixed stdout/stderr split, the event-payload principle, byte-identical notes body via writeNotesBody, full gating orthogonality with fail-loud forbidden-combination handling, render-only Prompt with no os/exec import, all four gate variants, and the verb-shaped cross-verb closing forms all conform. Project conventions hold. The single low-severity observation about start-line wording latitude is a recurrence of the cycle-1/cycle-2 recorded-only note — no change required.
