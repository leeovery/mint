AGENT: architecture
FINDINGS:
- FINDING: No end-to-end integration test drives a full worked-example run through either presenter
  SEVERITY: medium
  FILES: internal/presenter/plain_test.go:70, internal/presenter/pretty_test.go:76, internal/presenter/wiring_test.go:108
  DESCRIPTION: Every cross-task seam is exercised, but no test drives the complete worked-example sequence end-to-end and asserts the whole rendered transcript. The richest sequence tests are TestPlainPresenterRendersMinimalSequence (RunStarted → one StageSucceeded → RunFinished) and the pretty equivalent — three events. The spec ships two fully-worked transcripts (the plain `-y` agent run and the pretty human run, each ~15 events: RunStarted, version/preflight stages, ShowPlan, blocking notes stage, ShowNotes, the gate, record/tag/push/publish stages, RunFinished). Those transcripts are the contract for how the per-event renderings COMPOSE — inter-line spacing, the notes block sitting between the plan and the gate, the gate echo landing in the run's stage column, the footer following the last stage. Each event is unit-tested in isolation, and the wiring tests assert stream-split behaviour across 2-4 events, but nothing proves the events assemble into the spec's exact multi-line transcript. This is the one place a composition regression (a stray blank line between blocks, a column drift between a stage line and the `-y` echo, ordering coupling) would slip through every existing assertion. The pieces are individually verified; their assembly is not.
  RECOMMENDATION: Add one golden-transcript test per mode driving the spec's full worked example (the plain `-y` run and the pretty run) and asserting the output against the spec's transcript block. This pins the composition of the per-event renderings, not just each event alone, and turns the spec's worked examples into executable contracts. (Note: the plain `-y` transcript is the most byte-stable target; the pretty transcript carries the async spinner, so drive it with a spy/no-op spinner and/or assert the stable lines, not live animation frames.)
- FINDING: Choice type conflates a fixed semantic vocabulary with open enumerated values, pushing an ASCII-safety invariant onto uncheckable caller discipline
  SEVERITY: low
  FILES: internal/presenter/gate.go:9, internal/presenter/gate.go:193, internal/presenter/gate.go:219, internal/presenter/prompt.go:52
  DESCRIPTION: Choice is documented as "the engine's vocabulary" — the four fixed semantic answers y/n/e/r — and its distinct string type exists so a gate answer cannot be confused with arbitrary text at a call boundary. But SourceGate/TargetGate reuse the SAME Choice type to carry arbitrary engine-enumerated values ("github", "gitlab", "stable", "beta") as both keys and AcceptEcho. This makes Choice two things at once: a closed semantic enum and an open value container. The consequence is a correctness invariant that cannot be enforced by the type: SourceGate's doc states the supplied keys and default "MUST be ASCII enumerated values only" because parseChoice case-folds via strings.ToLower and AcceptEcho = string(def) must stay byte-pure for the plain echo — and explicitly notes "This contract is NOT enforced here." This was already surfaced and DOCUMENTED in analysis cycle 1 (task cli-presentation-5-4). Working as-is today because the source/target enumerations are ASCII.
  RECOMMENDATION: Working as-is; recorded for awareness. No restructuring needed unless source/target values broaden beyond ASCII. (Already documented at the SourceGate/TargetGate constructors per cycle-1 task 5-4; optional future hardening would validate the ASCII precondition at construction, but it is not required.)
SUMMARY: The package architecture is strong — a clean single-method-set Presenter seam, well-isolated pure cores (SelectMode, ruleWidth, parseChoice) separated from their OS probes, genuinely shared rendering helpers (writeNotesBody, readChoice/parseChoice) that prevent plain/pretty drift, and a properly-segregated recording double in its own subpackage. The two findings are a missing full-sequence integration test that would pin how the well-tested per-event renderings compose into the spec's worked transcripts (actionable), and a low-severity, already-documented overload of the Choice type that pushes an ASCII invariant onto unenforced caller discipline (recorded-only, working as-is).
