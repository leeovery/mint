AGENT: architecture
FINDINGS:
- FINDING: Two construction seams (`New` and `NewForStartup`) diverge in which signals they thread, and only one is production-reachable
  SEVERITY: low
  FILES: internal/presenter/wiring.go:19, internal/presenter/wiring.go:62
  DESCRIPTION: `New(mode, out, err)` documents itself as "the single wiring point where mode selection and the stream split meet" and claims "the split is established in exactly one place", but `NewForStartup` is a second, fuller construction site that additionally threads term-width, `-y`, and the stdin-interactive signal. `New` is referenced only by tests (wiring_test.go `newSplit`); the production path is `NewForStartup`. The two seams compose the pretty presenter differently — `New` builds `NewPrettyPresenter(out, WithErr(err))` with no width/gating, while `NewForStartup` adds `.WithTermWidth(...).WithYes(...).WithInteractiveStdin(...)`. This is a latent drift risk: a presenter built via `New` silently lacks gating arming, so the "single wiring point" claim in its doc comment is no longer accurate, and a future caller reaching for `New` would get a presenter that fails-open on the forbidden-combination rule. Given engine/main wiring is a deliberately-later epic this is low impact today, but the divergence is worth consolidating so there is genuinely one production seam. (NOTE: this is the direct doc-accuracy consequence of analysis-cycle-3 task 7-2, which made `NewForStartup` the fuller seam but left `New`'s "single wiring point" doc claim stale.)
  RECOMMENDATION: Either have `NewForStartup` delegate the mode-branch + stream-split to `New` (so the split truly lives in one place and `New` stays the shared core both paths flow through), or down-scope `New`'s doc comment to "test/raw wiring seam" and point the "single production construction site" language exclusively at `NewForStartup`. No behaviour change required — this is about keeping the one-place invariant the comments assert actually true.
SUMMARY: The presenter architecture is sound and well-composed — a clean dependency-inversion seam, strongly-typed payloads (distinct Choice/Mode/RunVerb/InitAction types, no untyped any at boundaries), orthogonal gating/render axes kept as separate fields, and shared writeNotesBody/readChoice/parseChoice cores that provably prevent the two render modes from drifting. The only structural nit is two construction seams whose "single wiring point" claim no longer holds (a doc-accuracy consequence of cycle-3 task 7-2).
