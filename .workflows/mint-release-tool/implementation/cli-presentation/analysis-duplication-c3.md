AGENT: duplication
FINDINGS:
- FINDING: Three independent "drive one Prompt across both modes" driver tables
  SEVERITY: medium
  FILES: prompt_test.go:40-55 (promptModeDriver/promptModeDrivers), prompt_render_only_test.go:117-154 (promptDriver/promptDrivers), gate_helpers_test.go:99-125 (gateDriver/gateDrivers)
  DESCRIPTION: Three separately-authored mode-driver abstractions now coexist for the same job — run one Prompt call against a plain and a pretty presenter and return the captured result so a mode-invariant property is asserted once per mode. promptDriver (prompt_render_only_test.go) and promptModeDriver (prompt_test.go) share the IDENTICAL struct shape: a `mode string` plus `run func(input string, gate presenter.Gate) (presenter.Choice, *bytes.Buffer, error)`. The prompt_test.go doc comment itself states "It mirrors promptDriver in prompt_render_only_test.go" — an explicit acknowledgement of the copy. gateDriver (gate_helpers_test.go) is the third variant of the same concept with a slightly different signature (it threads gateOpts and returns a gateResult struct). Each was written in isolation per task/phase and they now drift independently: promptDrivers pins the pretty arm to TrueColor, promptModeDrivers pins it to Ascii, gateDrivers pins it to Ascii. The capture-and-Prompt loop body is repeated verbatim across all three.
  RECOMMENDATION: Consolidate onto the most general of the three. gateDriver/gateDrivers in gate_helpers_test.go is already the richest (it carries gateOpts arming and the out+err buffers in gateResult) and lives in the shared helpers file. Have prompt_test.go and prompt_render_only_test.go reuse a single shared mode-driver table (parameterising the forced colour profile where a test genuinely needs TrueColor vs Ascii) instead of redeclaring promptDriver/promptModeDriver. This removes two of the three parallel struct+factory pairs and the duplicated Prompt-capture closures.
- FINDING: Four near-identical single-Prompt construct-and-capture helpers
  SEVERITY: medium
  FILES: prompt_test.go:18-34 (drivePlainPrompt/drivePrettyPrompt), pretty_gate_test.go:18-23 (drivePrettyPromptProfile), gate_helpers_test.go:53-78 (plainGate/prettyGate)
  DESCRIPTION: The same idiom — allocate out (and err) buffers, build a Plain/Pretty presenter with an injected strings.Reader script, call Prompt once, return the captured buffer(s) — is hand-written in four places. drivePrettyPrompt (prompt_test.go) and drivePrettyPromptProfile (pretty_gate_test.go) differ only in that one pins the Ascii profile and the other takes the profile as a parameter; drivePrettyPromptProfile is strictly the more general form, so drivePrettyPrompt is a redundant specialisation of it. plainGate/prettyGate in gate_helpers_test.go already centralise this exact construction (buffers + presenter + arming) but return the presenter rather than driving Prompt, so the prompt-test helpers re-implement the construction half a second time. This is copy-paste drift across the gate/prompt task boundary.
  RECOMMENDATION: Build the prompt drivers on top of the existing plainGate/prettyGate construction seam rather than re-allocating buffers and re-calling the constructors inline. Replace drivePrettyPrompt with a call to the profile-taking drivePrettyPromptProfile (passing termenv.Ascii) so there is one pretty-prompt driver, not two. This collapses the four construct-and-capture helpers toward the single plainGate/prettyGate seam the helpers file already established.
SUMMARY: Production code is well-factored — shared helpers (writeNotesBody, readChoice/parseChoice, bufferedReader, leafOrDefault, formatElapsed, IsTerminal) already eliminate the obvious cross-mode duplication. The remaining duplication is entirely on the test side: three parallel "Prompt-across-both-modes" driver tables and four near-identical single-Prompt construct-and-capture helpers, authored independently per phase and now overlapping, should be consolidated onto the existing gate_helpers_test.go construction seam.
