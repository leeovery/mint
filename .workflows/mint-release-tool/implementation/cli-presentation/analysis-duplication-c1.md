AGENT: duplication
FINDINGS:
- FINDING: Byte-purity ASCII-scan loop copy-pasted across 9 test functions in 5 files
  SEVERITY: medium
  FILES: internal/presenter/plain_test.go:479, internal/presenter/plain_test.go:534, internal/presenter/plain_test.go:743, internal/presenter/plain_test.go:827, internal/presenter/plain_test.go:950, internal/presenter/gate_skip_test.go:146, internal/presenter/init_test.go:108, internal/presenter/gate_sourcetarget_test.go:196, internal/presenter/gate_forbidden_test.go:74
  DESCRIPTION: The same ~12-line `for i, b := range ...Bytes() { switch { case b == 0x1b ... case b == 0x0d ... case b == '\n' ... case b < 0x20 || b > 0x7e ... } }` block that asserts the plain byte-purity contract (no ESC, no CR, only printable ASCII + newline) is reproduced verbatim nine times across five test files. The only variation between copies is the trailing descriptive string in the final case arm and, in two copies (plain_test.go:479 and gate_forbidden_test.go:74), an outer loop iterating over both the out and err buffers. Each copy was authored by the task executor that added the feature it guards (warn, unwound, init, regenerate close, plan, -y echo, forbidden combo, source/target echo), so they emerged independently rather than being shared. This is the package's single largest cross-file duplication and the byte-purity invariant now lives in nine places that must be kept in sync.
  RECOMMENDATION: Extract one shared test helper in the `presenter_test` package (e.g. `assertBytePureASCII(t *testing.T, buf *bytes.Buffer, context string)`) running the byte scan, plus a thin `assertBytePureASCIIStreams(t, context, bufs...)` wrapper for the two multi-stream call sites. Replace all nine inline blocks with a single call each, passing the per-site context string. This consolidates the contract to one definition with no behavioural change.
SUMMARY: The production presenter code is already well-factored (shared writeNotesBody, readChoice/parseChoice, bufferedReader, formatElapsed, leafOrDefault, stripANSI), so no production duplication warrants extraction. The one significant cross-file duplication is in tests: a ~12-line byte-purity ASCII-scan loop independently copy-pasted into nine test functions across five files.
