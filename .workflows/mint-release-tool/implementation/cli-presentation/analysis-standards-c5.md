AGENT: standards
FINDINGS:
- FINDING: Substring assertions used where exact output is deterministic
  SEVERITY: low
  FILES: internal/presenter/init_test.go:176, internal/presenter/init_test.go:179, internal/presenter/init_test.go:199, internal/presenter/init_test.go:202, internal/presenter/pretty_gate_test.go:81, internal/presenter/pretty_gate_test.go:121
  DESCRIPTION: code-quality.md lists "Substring assertions in tests when exact output is deterministic" as an anti-pattern. Several positive assertions use strings.Contains on individual line fragments (e.g. init created/skipped lines, the gate default marker) where the full line is fully deterministic and could be asserted with exact equality. This is materially mitigated: golden_transcript_test.go pins the full composed transcript byte-for-byte for both modes, plain_test.go/pretty_test.go assert exact per-event lines, and many Contains calls in these files are legitimate negative/absence assertions (e.g. "Continue?" must NOT appear under -y) for which Contains is the correct tool. The remaining positive fragment-Contains cases are a minor rigour gap, not a behavioural drift.
  RECOMMENDATION: Where a positive assertion targets a fully deterministic single line, prefer exact equality on that line (or HasSuffix on the complete line, as init_test.go:314 and :340 already do well) rather than a Contains on a fragment. Leave the negative/absence Contains checks as-is — they are correct.
SUMMARY: The implementation conforms closely to the CLI-presentation specification and the project's Go conventions: the Presenter seam, event-payload principle, mode-selection precedence (no env sniffing), the two orthogonal gating axes, the forbidden-combination fail-loud path, the success-only suppression of the end-of-run line, the non-negotiable byte-identical notes body, the no-alt-screen linear-narration constraint, and the stdout/stderr stream split are all honoured. Spec-granted latitude (flush-left notes rules, stage-agnostic "running..." / spinner start text) is correctly exercised. The only finding is a low-severity test-rigour nuance, well-mitigated by the byte-exact golden tests.
