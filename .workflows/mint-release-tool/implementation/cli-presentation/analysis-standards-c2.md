AGENT: standards
FINDINGS:
- FINDING: Plain long-stage start line is stage-agnostic ("running...") rather than the spec's stage-specific verb ("generating…")
  SEVERITY: low
  FILES: internal/presenter/plain.go:152-157
  DESCRIPTION: The spec's Plain Layer shows the long/blocking start line as a stage-specific verb (`notes: generating…` → `notes: generated (1.1s)`). The implementation emits a fixed stage-agnostic `"{name}: running..."`. The reasoning is sound and explicitly documented: the StageStart payload carries only Name + Blocking, so per the event-payload principle the presenter must not invent stage-specific narration (a "generating" verb would only be correct for the notes stage, not the pre_tag build hook). The spec also grants "wording refinable" latitude. This is a defensible interpretation of two competing spec rules (worked-example wording vs the payload principle), and the payload principle correctly won — but it is a visible divergence from the literal worked example, so flagging for awareness. No change required unless the team wants the engine to supply a start verb in the payload.
  RECOMMENDATION: No change required — the event-payload principle correctly supersedes the worked-example verb. Recorded for traceability only. (If the team later wants the spec's exact "generating…" wording, the engine must supply a start verb/detail on the StageStart payload.)
SUMMARY: Implementation conforms to the specification and project conventions across mode detection, the stream split, byte-identity of the notes body, gating orthogonality, the forbidden-combination fail-loud path, verb-shaped end-of-run dispatch, render-only Prompt, and the spinner lifecycle. The single low-severity note is the deliberate, well-reasoned choice of a stage-agnostic plain start line over the worked example's stage-specific verb — the right call given the event-payload principle, surfaced only for visibility (no change required).
