---
phase: 3
phase_name: Operator surfacing and contract reconciliation
total: 3
---

## ai-model-selection-3-1 | approved

### Task ai-model-selection-3-1: Scaffold the new `ai_command` / `timeout` keys in the initgen commented template

**Problem**: `internal/initgen`'s commented `.mint.toml` template still scaffolds the OLD shared default `ai_command = 'claude -p'` and has no `timeout` key at all, and it shows no per-verb override pattern under `[release]` / `[commit]`. After Phases 1–2 the shipped default is `claude -p --model sonnet`, `timeout` is a real shared-plus-per-verb config key (60s shipped default), and both keys accept per-verb overrides. The project convention is that every new config key appears in the scaffolded template and is user-facing-documented, so the template must surface these keys — and the spec mandates the pinned default *value* be sourced from the canonical config constant (`config.DefaultAICommand`), not re-typed, so the literal cannot drift from the schema's floor.

**Solution**: Update `internal/initgen/initgen.go`'s `MintTOML()` so the top-level `ai_command` shows `claude -p --model sonnet` (sourced from `config.DefaultAICommand`, not re-typed) and a new uncommented shared `timeout` key appears at its 60s default (sourced from / pinned-against `config.DefaultTimeout`); and add commented `# ai_command = …` / `# timeout = …` per-verb override lines under both `[release]` and `[commit]` with model-agnostic, latency-framed explanations. Because `initgen` today emits a PURE static string and does NOT import `config`, reconcile "sourced from the config constant, not re-typed" against that static design — either build the relevant template line(s) from the constant (`fmt.Sprintf` / string concatenation pulling `config.DefaultAICommand`), or keep the static string and add a test in `initgen_test.go` that pins the template's `ai_command` line equals `config.DefaultAICommand` (and the timeout line equals `config.DefaultTimeout` in seconds), so a drift between template and constant fails the build. Update the test fixtures that pin the old `claude -p` default.

**Outcome**: The scaffolded template's active top-level keys read `ai_command = 'claude -p --model sonnet'` and `timeout = 60`; commented `# ai_command = …` and `# timeout = …` per-verb overrides appear under both `[release]` and `[commit]` with model-agnostic explanations (timeout hint framed around command latency); the template's pinned default value is provably tied to `config.DefaultAICommand` (built from it or test-pinned against it, never an independent literal that can silently drift); and `TestMintTOML_UncommentedLoadsCleanly` plus the common/optional-key tests pass against the updated template and the Phase 1 schema.

**Do**:
- In `internal/initgen/initgen.go`, change the active top-level line `ai_command = 'claude -p'` (~line 39) to `ai_command = 'claude -p --model sonnet'`. To satisfy "sourced from the config constant, not re-typed": import `mint/internal/config` and build that line from `config.DefaultAICommand` (e.g. `fmt.Sprintf("ai_command = '%s'\n", config.DefaultAICommand)` woven into the template) OR keep the literal in the static string but add a build-failing test pin (see Tests) that asserts the template's emitted `ai_command` value equals `config.DefaultAICommand`. Pick ONE mechanism and make the value-tied-to-constant guarantee real; do NOT leave a bare hand-typed literal with no link to the constant. If you keep the string pure-static for readability, the test pin is mandatory.
- Add a new active top-level shared key `timeout = 60` immediately after `ai_command` (in the "Engine-level keys (shared by every mint verb)" block, ~line 39–41), at the 60s shipped default. The value is integer seconds (Phase 1's chosen representation). Tie the `60` to `config.DefaultTimeout` the same way you tied `ai_command` to `config.DefaultAICommand` (build it via `int(config.DefaultTimeout / time.Second)`, or pin it in a test) so the scaffolded timeout cannot drift from the schema floor. Add a model-AGNOSTIC trailing explanation framed around command latency — e.g. `timeout = 60  # per-attempt AI deadline in seconds; raise it if your ai_command runs slowly (0 = no limit)`. Do NOT name any model (no sonnet/opus/haiku), do NOT steer toward "a stronger model", and do NOT put a concrete model-tied timeout number in the hint beyond the generic 60s default value itself.
- Add commented per-verb override lines under `[release]` (in its commented-overrides block, ~lines 51–56) and under the `[commit]` block (~lines 66–68). Under each table add `# ai_command = 'claude -p --model sonnet'  # override the AI command for this verb only` and `# timeout = 120  # override the per-attempt deadline (seconds) for this verb only; raise it if this verb's ai_command runs slowly` (the `120` example is illustrative, not a recommendation — choose a schema-valid positive integer distinct from 60 so it reads as an override example). Keep both lines model-agnostic (the example command value may carry `--model sonnet` as the decided default value, but the explanatory comment must not name or steer a model). Both per-verb lines must follow the template's existing optional-key convention: a single leading `#` marker, a `key = value` config line, and a trailing `# …` explanation (so `TestMintTOML_OptionalKeysEachHaveAComment`-style pins and `isConfigLineForKey`/`hasExplainedCommentedKey` recognise them) — and the `[commit]` table currently has its HEADER commented (`# [commit]`), so the per-verb override lines under it must be commented one level the same as the existing `# context` / `# prompt` lines so that `uncommentTemplate` produces a still-valid document.
- Update `internal/initgen/initgen.go`'s package/`MintTOML` WHY-comments (~lines 1–30) only if a behaviour they describe changed — specifically, if you import `config` to source the default value, the doc comment's "DELIBERATELY static — initgen does NOT sniff package.json or any project file to pre-fill values" claim is still TRUE (sourcing a compiled Go constant is not project sniffing), but add a brief note that the pinned default *value* is sourced from `config.DefaultAICommand` / `config.DefaultTimeout` (the canonical schema), keeping the comment true-to-as-built per CLAUDE.md. Do NOT weaken the "no project auto-detection / static template" contract.
- Update `internal/initgen/initgen_test.go`: in `commonKeyDefaults` (~line 18), change `"ai_command = 'claude -p'"` to `"ai_command = 'claude -p --model sonnet'"` and add `"timeout = 60"` as an active common key line. In `TestMintTOML_UncommentedLoadsCleanly` (~line 256), change the sanity assertion `cfg.AICommand != "claude -p"` to assert `cfg.AICommand == "claude -p --model sonnet"` (the new active default), and add a sanity assertion that the uncommented document's resolved timeout is 60s (e.g. assert `cfg.TimeoutFor(config.VerbRelease)` / the shared timeout resolves to 60s) — this proves the new `timeout = 60` line round-trips through the real `config.Load` and the Phase 1 schema. Add `timeout` to `optionalKeys` / `keysNeedingInlineComment` ONLY for the per-verb commented forms if the per-verb lines are the ones being asserted; if the table-level assertions already iterate the relevant keys, ensure the new per-verb `ai_command`/`timeout` commented lines are covered (a focused new test is cleaner than overloading the existing key lists — see Tests).
- Verify the uncomment round-trip: `uncommentTemplate` strips ONE leading `#` from each commented config line. With per-verb `# ai_command = …` / `# timeout = …` under `[release]` (uncommented table) and under `# [commit]` (commented table header that `uncommentTemplate` also uncomments), the result must still be a valid, strict-decoding document — both verbs accept `ai_command`/`timeout` after Phase 1. Run `TestMintTOML_UncommentedLoadsCleanly` to confirm no unknown-key / bad-type / duplicate-key error.

**Acceptance Criteria**:
- [ ] The template's active top-level `ai_command` line reads `claude -p --model sonnet`, and its value is sourced from / pinned against `config.DefaultAICommand` (built from the constant or asserted equal by a build-failing test), never a free-floating literal.
- [ ] The template adds an active top-level shared `timeout` key at the 60s default (integer seconds), tied to `config.DefaultTimeout` the same way.
- [ ] Commented `# ai_command = …` and `# timeout = …` per-verb override lines appear under BOTH `[release]` and `[commit]`, each a single-`#` config line with a trailing `# …` explanation.
- [ ] All template comments touching the new keys are model-agnostic: no `sonnet`/`opus`/`haiku` naming in any explanation, no "use a stronger model" steer, and the timeout hint is framed around command latency, not a model, with no concrete model-tied timeout number.
- [ ] `TestMintTOML_UncommentedLoadsCleanly` passes against the Phase 1 schema and asserts `cfg.AICommand == "claude -p --model sonnet"` and a resolved 60s timeout.
- [ ] `commonKeyDefaults` includes the new `ai_command` and `timeout` active lines; the common/optional-key tests pass.
- [ ] The `initgen` package/`MintTOML` doc comments stay true-to-as-built (the "static template, no project auto-detection" contract is preserved; the constant-sourcing is noted if `config` is imported).
- [ ] `go build ./...`, `gofmt -l .` (prints nothing), `go vet ./...`, `go test -race ./...`, `golangci-lint run` (0 issues) all pass.

**Tests**:
- `"it scaffolds the top-level ai_command at the pinned default claude -p --model sonnet"` — assert the template contains the active line `ai_command = 'claude -p --model sonnet'`.
- `"its scaffolded ai_command default equals config.DefaultAICommand (no drift)"` — extract the template's emitted `ai_command` value and assert it equals `config.DefaultAICommand`; this is the build-failing drift pin if the static-string mechanism is chosen.
- `"it scaffolds an active shared timeout key at the 60s default"` — assert the template contains `timeout = 60` as an active (uncommented) line, and (if test-pinned) that it equals `int(config.DefaultTimeout / time.Second)`.
- `"it shows commented per-verb ai_command and timeout overrides under both [release] and [commit]"` — assert each of `[release]` and `[commit]` carries a commented `ai_command = …` and `timeout = …` config line with a trailing explanation.
- `"its config comments name no model and no stronger-model steer"` — scan every comment line and assert none contain `sonnet`/`opus`/`haiku` as an explanation token (the pinned default VALUE `--model sonnet` on a config line is allowed; a model name inside a `# …` explanation is not) and none contain a "stronger model"-style steer.
- `"the timeout hint is framed around command latency, not a model"` — assert the timeout explanation references command/ai_command latency wording and carries no concrete model-tied timeout number.
- `"the uncommented template still loads cleanly through config.Load with the new keys"` — `TestMintTOML_UncommentedLoadsCleanly`, extended to assert the new active default value and 60s timeout round-trip.

**Edge Cases**:
- Pinned value sourced from the config constant, not re-typed (per the task table) — built from `config.DefaultAICommand` or asserted equal by a drift test.
- Comments stay model-agnostic — no sonnet/opus/haiku and no "stronger model" steer in any explanation (the default VALUE carrying `--model sonnet` is allowed; explanations are model-free).
- Timeout hint framed around command latency, not a model, with no concrete model-tied timeout number.
- Per-verb overrides shown commented (optional → commented) — both per-verb lines follow the single-`#` config-line + trailing-explanation convention so the optional-key pins recognise them and `uncommentTemplate` yields a still-valid document.
- The "full template loads cleanly" sanity pin updated to the new default — `TestMintTOML_UncommentedLoadsCleanly`'s `cfg.AICommand != "claude -p"` assertion becomes `== "claude -p --model sonnet"`.
- Uncomment-loads-cleanly still passes against the Phase 1 schema — the commented per-verb `ai_command`/`timeout` lines under `[release]` and `[commit]` decode without unknown-key/bad-type/duplicate-key errors.

**Context**:
> Spec — Init template scaffolding: "Top-level (uncommented, matching other defaulted shared keys): bump the scaffolded `ai_command` to `claude -p --model sonnet` — the pinned default *value*, sourced from the config constant, not re-typed; add the new shared `timeout` key at its 60s default. Per-verb overrides shown commented under both `[release]` and `[commit]` — `# ai_command = …` and `# timeout = …` — so the override pattern is discoverable (optional → commented, per the template's own convention)."
> Spec — Init template scaffolding: "Config comments stay model-agnostic. Comments describe what a key does and never name a specific model (no sonnet/opus/haiku, no 'use a stronger model' steer). The `timeout` hint is framed around *command latency*, not a model — e.g. 'raise if your `ai_command` runs slowly.' The pinned default *value* still carries `--model sonnet` (that is the decided default value, not a comment). No concrete, model-tied timeout number appears in the scaffold; the mitigation hint is generic and model-free."
> Spec — Single source of truth for config defaults: "`initgen` pulls its template values from `config` rather than re-typing literals (the pinned default *value* is sourced from the config constant, not re-typed)." De-duplication target: "After this work the value lives canonically in `internal/config` and the other sites derive from it."
> Phase 1 contracts consumed: `config.DefaultAICommand = "claude -p --model sonnet"` (exported), `config.DefaultTimeout = 60 * time.Second` (exported), integer-seconds `timeout` representation, per-verb `ai_command`/`timeout` keys on both `[release]` and `[commit]` shapes, `config.VerbRelease` / `config.TimeoutFor` for the round-trip assertion.
> As-built (must reconcile): `internal/initgen/initgen.go`'s `MintTOML()` returns a PURE static string and does NOT currently import `config` — the package doc comment stresses the template is "DELIBERATELY static … initgen does NOT sniff package.json or any project file to pre-fill values." Sourcing a compiled Go constant is NOT project sniffing, so "sourced from the config constant" can be satisfied either by importing `config` and building the line from `config.DefaultAICommand`, or by keeping the static string and adding a build-failing test that pins the template value equals the constant. The init tests (`initgen_test.go`) pin the old default: `commonKeyDefaults` (~line 18) lists `ai_command = 'claude -p'`; `TestMintTOML_UncommentedLoadsCleanly` (~line 256) asserts `cfg.AICommand != "claude -p"`; the optional-key tests (`TestMintTOML_OptionalKeysPresentButCommentedWithExplanation` / `…EachHaveAComment`) pin the single-`#` config-line + trailing-`# …` convention via `isConfigLineForKey` / `hasExplainedCommentedKey`. The `[commit]` table header itself is commented (`# [commit]`), and `uncommentTemplate` strips one `#` from every config line including the header.
> CLAUDE.md: "New config keys must also be added to the commented template in `internal/initgen`" and comments must stay true-to-as-built in the same change.

**Spec Reference**: `.workflows/ai-model-selection/specification/ai-model-selection/specification.md` — Init template scaffolding; Single source of truth for config defaults (initgen pulls template values from config; de-duplication target).

## ai-model-selection-3-2 | approved

### Task ai-model-selection-3-2: Document the new config surface in the README

**Problem**: The README documents the OLD config surface — the Shared engine keys table lists `ai_command` with default `claude -p` and no `timeout` row, the scaffold snippet shows `ai_command = 'claude -p'` with no `timeout`, neither verb table mentions per-verb `ai_command` / `timeout` overrides, and "The AI Transport" section hard-codes the 60s deadline as immutable and shows `claude -p` as the default. After Phases 1–2 the shipped default is `claude -p --model sonnet`, `timeout` is a tunable shared-plus-per-verb key with an operator-chosen `timeout = 0` ⇒ "no time limit" semantics, resolution is `verb → shared → default`, and overriding command+timeout together for a slow verb is the supported (but unenforced) pattern. The user-facing docs must describe the as-built behaviour, or operators cannot discover or correctly use the new surface.

**Solution**: Update `README.md`'s Configuration section (the scaffold snippet, the Shared engine keys table, and the `[release]` / `[commit]` tables) and the "The AI Transport" section to document: the new pinned default `claude -p --model sonnet` stated as a FACT (not a recommendation), `ai_command` at both levels, `timeout` at both levels, the `verb → shared → default` resolution order, the `timeout = 0` ⇒ no-deadline semantics including the unbounded-call trade-off, and the supported-but-unenforced pattern of overriding command and timeout together for a slow verb.

**Outcome**: The README's config tables and AI Transport section describe the new keys, the resolution order, the new pinned default (as a fact), the `timeout = 0` no-limit trade-off, and the override-both-for-a-slow-verb pattern (supported but unenforced) — all matching the as-built Phases 1–2 behaviour, with the scaffold snippet matching the updated initgen template from Task 3-1.

**Do**:
- In `README.md`'s Configuration scaffold snippet (~lines 173–203), change `ai_command = 'claude -p'` to `ai_command = 'claude -p --model sonnet'` and add `timeout = 60` to the Engine-level keys block (mirroring the Task 3-1 initgen template so the README snippet and the scaffold agree). Add commented `# ai_command = …` / `# timeout = …` per-verb lines under `[release]` and `[commit]` if you keep the snippet faithful to the template (optional but recommended for parity with Task 3-1).
- In the "Shared engine keys" table (~lines 207–211): change the `ai_command` row's Default cell from `claude -p` to `claude -p --model sonnet`, and add a `timeout` row — Default `60`, Description framed around command latency and the resolution/`0`-semantics (e.g. "per-attempt AI deadline in seconds; `0` = no limit; resolves `[verb] → shared → default`"). State the pinned default as a fact, not a recommendation (no "we recommend sonnet" wording).
- In the `[release]` table (~lines 213–228) and the `[commit]` table (~lines 238–243): add `ai_command` and `timeout` rows documenting them as OPTIONAL per-verb overrides (Default cell: "shared" or "inherits shared", Description: "override the AI command/deadline for this verb only; resolves `[verb] → shared → default`"). This is the only place the per-verb override is tabular-documented for each verb.
- Document the resolution order explicitly once (in the Shared engine keys preamble or the AI Transport section): `ai_command` and `timeout` each resolve per-verb-independently as `[verb].<key> → top-level shared <key> → shipped default`.
- In "The AI Transport" section (~lines 247–257): (a) update the snippet so `ai_command = 'claude -p --model sonnet'` is shown as "# the default" (it already shows `claude -p --model sonnet` as a "pin a model" example — reconcile: the default line becomes `claude -p --model sonnet`; the separate "pin a model" example can show a DIFFERENT model command or be folded, but do NOT name a recommended model as a steer — present `--model sonnet` as the shipped default fact); (b) replace "The transport applies a 60s per-attempt deadline" with wording that states 60s is the DEFAULT deadline, tunable via `timeout`, AND document that `timeout = 0` disables the per-attempt deadline entirely — the AI call then runs UNBOUNDED (the conscious trade-off, a deliberate exception to mint's "fail loud, never hang" posture, operator-chosen); (c) document the supported-but-unenforced pattern: when you pin a slower model for a verb, raise that verb's `timeout` in the same `[verb]` table — mint does NOT auto-bump the timeout, warn, or require the pair, so a slow command under the 60s default will hit the fatal per-attempt deadline; overriding both keys together is the supported pattern but is NOT enforced.
- State the new pinned default as a FACT throughout (no "recommended", no "we suggest", no "for best results use …"). The `timeout = 0` trade-off and the override-both pattern are described as behaviour/guidance, not as a model recommendation.
- Do NOT document any environment-variable override layer, driver/provider-registry for AI, or interactive `mint init` prompting — all explicitly out of scope (non-goals). Keep the docs to the two-layer model (compiled defaults ← project file).

**Acceptance Criteria**:
- [ ] The Shared engine keys table's `ai_command` default is `claude -p --model sonnet` and a `timeout` row exists (default `60`, latency-framed description), with the new default stated as a fact (no recommendation wording).
- [ ] The `[release]` and `[commit]` tables each document `ai_command` and `timeout` as optional per-verb overrides resolving `[verb] → shared → default`.
- [ ] The README states the `verb → shared → default` resolution order explicitly for both keys.
- [ ] "The AI Transport" section documents `timeout = 0` ⇒ no per-attempt deadline / unbounded AI call, including the conscious trade-off (the deliberate exception to "fail loud, never hang").
- [ ] The README documents the supported-but-unenforced pattern of overriding `ai_command` and `timeout` together for a slow verb (mint does not auto-bump, warn, or require the pair).
- [ ] The scaffold snippet in Configuration matches the Task 3-1 initgen template (`ai_command = 'claude -p --model sonnet'`, `timeout = 60`).
- [ ] No environment-variable override, AI driver/registry, or interactive-init scaffolding is documented (out of scope per non-goals); the two-layer (defaults ← project file) model is preserved.
- [ ] `go build ./...`, `gofmt -l .` (prints nothing), `go vet ./...`, `go test -race ./...`, `golangci-lint run` (0 issues) all pass (README-only change; the gates must remain green).

**Tests**:
- This is a documentation task with no Go test harness for prose. Verification is by review against the acceptance criteria and by the gates remaining green. Concretely the reviewer confirms: (1) the `ai_command` default reads `claude -p --model sonnet` in both the snippet and the Shared engine keys table; (2) a `timeout` row exists at default `60` with a latency-framed, model-free description; (3) the `[release]` and `[commit]` tables each carry `ai_command` + `timeout` override rows naming the `[verb] → shared → default` order; (4) the AI Transport section documents `timeout = 0` ⇒ unbounded with the explicit trade-off; (5) the override-both-for-a-slow-verb pattern is documented as supported-but-unenforced; (6) the new default is stated as a fact, with no model recommendation/steer anywhere.
- If the repo has any doc-link or markdown-lint check wired into the gates, it must pass against the edited README (there is no Go unit test that asserts README content).

**Edge Cases**:
- Pinned default stated as a fact, not a recommendation (per the task table) — no "recommended"/"we suggest"/"for best results" wording anywhere the default is mentioned.
- `timeout = 0` ⇒ no-deadline unbounded-call trade-off documented (per the task table) — including that it is a deliberate, operator-chosen exception to "fail loud, never hang".
- Override-both-keys-for-a-slow-verb documented as supported-but-unenforced (per the task table) — mint does not auto-bump, warn, or require the pair; the operator owns it.

**Context**:
> Spec — README documentation: "The README documents: the new keys (`ai_command` at both levels, `timeout` at both levels), the `verb → shared → default` resolution order, the new shipped default value (`claude -p --model sonnet`) — stated as a fact, not a recommendation, the `timeout = 0` ⇒ 'no time limit' semantics, including the unbounded-call trade-off, the supported (unenforced) pattern of overriding command and timeout together for a slow verb. No breaking-change callout is needed — mint has no users yet."
> Spec — Resolution value semantics (`timeout`): "Zero is an explicit, honored value meaning 'no time limit' — it disables the per-attempt deadline and stops the fall-through … This is a conscious, operator-chosen exception to 'fail loud, never hang': the operator is opting into an AI call that can run unbounded. It must be documented, including that trade-off."
> Spec — Timeout × model-choice coupling — operator's responsibility: "a verb that overrides `ai_command` to a slower model but not `timeout` silently inherits the 60s shared default — the exact fatal-deadline exposure the override exists to prevent. Mint does not protect against this: no auto-bump, no warning, no paired-defaults requirement. If you slow the model, you raise the timeout yourself. The supported pattern — override both keys together for a slow verb — is documented (README/spec) but not enforced. Mint ships the current 60s as the shared default."
> Spec — Scope boundaries (non-goals): No environment-variable override layer; no driver/provider-registry pattern; no interactive `mint init` prompting. Two layers only: compiled defaults ← project file. The README must not document any of these out-of-scope mechanisms.
> Spec — Pinned default model: "The shipped default command becomes `claude -p --model sonnet` … Default model is Sonnet … Opus is reserved for explicit per-verb opt-in — never the shipped default." The README states the default as a fact; it does not recommend a model.
> As-built: `README.md` Shared engine keys table (~lines 205–211) lists `ai_command | claude -p`; the `[release]` (~213–228) and `[commit]` (~238–243) tables have no `ai_command`/`timeout` rows; "The AI Transport" (~247–257) shows `claude -p` as "# the default", `claude -p --model sonnet` as a "pin a model" example, and states "The transport applies a 60s per-attempt deadline" as immutable. The Configuration scaffold snippet (~173–203) shows `ai_command = 'claude -p'` and no `timeout`.

**Spec Reference**: `.workflows/ai-model-selection/specification/ai-model-selection/specification.md` — README documentation; Resolution value semantics (`timeout`, the unbounded-call trade-off); Timeout × model-choice coupling — operator's responsibility; Pinned default model; Scope boundaries (non-goals).

## ai-model-selection-3-3 | approved

### Task ai-model-selection-3-3: Reconcile the `Commit` struct doc comment to the shipped per-verb override

**Problem**: Phase 1 (Task 1-3) added `[commit].ai_command` and `[commit].timeout` to the schema, and Phase 2 (Task 2-4) wired `mint commit` to resolve through them — so the commit verb now HAS per-verb engine-override keys. But the in-repo comments in `internal/config/config.go` still encode the OLD "no per-verb override" contract: the `Config.Commit` FIELD comment says `No push/scope/per-verb-engine override keys exist for commit (spec: "Deliberately NOT added for commit")`, and the `Commit` struct doc comment describes commit as having only its two prompt knobs with no engine-override keys. CLAUDE.md requires comments stay true-to-as-built IN THE SAME CHANGE; leaving these comments asserting a contract the shipped code contradicts is exactly what CLAUDE.md forbids. (Task 1-3 deliberately deferred this full reconciliation to Phase 3, touching the comment then only as far as compilation forced.)

**Solution**: Update the `Config.Commit` field comment and the `Commit` struct doc comment in `internal/config/config.go` so they no longer assert "Deliberately NOT added for commit" / the no-per-verb-override contract — instead recording that `[commit]` now carries `ai_command` and `timeout` per-verb overrides (resolving `[commit] → shared → default`) alongside its two prompt knobs, mirroring `[release]`. This is the in-repo comment reconciliation ONLY; the external commit-command spec DOCUMENT revision is explicitly out of scope (a separate commit-spec pass).

**Outcome**: The `Config.Commit` field comment and the `Commit` struct doc comment in `internal/config/config.go` reflect the as-built shipped surface — `[commit]` has per-verb `ai_command` / `timeout` overrides (plus its two prompt knobs), with no residual "Deliberately NOT added for commit" or "no per-verb-engine override keys exist for commit" claim; the external commit-command spec document is untouched (deferred to a separate pass).

**Do**:
- In `internal/config/config.go`, rewrite the `Config.Commit` FIELD comment (~lines 83–89, currently ending `No push/scope/per-verb-engine override keys exist for commit (spec: "Deliberately NOT added for commit").`). Drop the "Deliberately NOT added for commit" / "No … per-verb-engine override keys exist for commit" sentence. State the as-built: `[commit]` carries its two prompt knobs (Context, Prompt) AND the per-verb engine overrides `ai_command` / `timeout`, which resolve `[commit] → shared top-level → shipped default` (mirroring `[release]`); commit still reads the OTHER shared engine keys (`diff_exclude`, `max_diff_lines`) from the top level (those stayed shared-only). Keep the comment accurate about which keys are per-verb (`ai_command`, `timeout`) versus shared-only (`diff_exclude`, `max_diff_lines`).
- Rewrite the `Commit` STRUCT doc comment (~lines 169–176) so it no longer implies commit has ONLY its two prompt knobs. Note that the `Commit` struct's carried fields now include the per-verb `ai_command` / `timeout` override values (added in Tasks 1-3 / 1-6) in addition to Context and Prompt — describe each field's role briefly (the prompt knobs as before; the per-verb overrides as the carried raw values the `AICommandFor`/`TimeoutFor` accessors resolve through). Keep the comment true to the actual struct fields as they exist after Phase 1 (which added the carried override fields to `Commit`) — read the current field list before writing so the comment matches the as-built field set exactly.
- Verify against the as-built struct: read the `Commit` struct definition as it stands after Phase 1 (it may now carry `ai_command`/`timeout` raw fields beyond `Context`/`Prompt`) and ensure the rewritten doc comment enumerates exactly the fields present — do NOT describe fields that do not exist, and do NOT omit the per-verb override fields if they are carried on `Commit`.
- Do NOT edit the external commit-command specification document (the one carrying "Deliberately NOT added for commit … promote to a `[commit]` key only if a real need appears"). That document revision is a SEPARATE commit-spec pass, explicitly out of this work unit's scope. This task changes ONLY the in-repo comments in `internal/config/config.go`.
- Do NOT re-open Phase 1/2 work: the schema fields, accessors, and wiring already exist; this task is comment-only reconciliation. No new config keys, no accessor changes, no test-behaviour changes are introduced here.

**Acceptance Criteria**:
- [ ] The `Config.Commit` field comment no longer contains "Deliberately NOT added for commit" or any "no per-verb(-engine) override keys exist for commit" assertion.
- [ ] The `Config.Commit` field comment states the as-built: `[commit]` carries per-verb `ai_command` / `timeout` overrides (resolving `[commit] → shared → default`) plus its Context/Prompt knobs, with `diff_exclude` / `max_diff_lines` still served shared-only from the top level.
- [ ] The `Commit` struct doc comment matches the as-built field set (Context, Prompt, and the per-verb override fields carried after Phase 1), with no residual no-per-verb-override claim and no description of non-existent fields.
- [ ] The external commit-command specification document is NOT modified (verified: only `internal/config/config.go` comments changed by this task).
- [ ] No schema, accessor, wiring, or test-behaviour change is introduced (comment-only reconciliation).
- [ ] `go build ./...`, `gofmt -l .` (prints nothing), `go vet ./...`, `go test -race ./...`, `golangci-lint run` (0 issues) all pass.

**Tests**:
- This is a comment-only reconciliation; there is no new behavioural test. Verification is by review and the gates: (1) `grep -n "Deliberately NOT added for commit" internal/config/config.go` returns nothing; (2) `grep -ni "no .*per-verb.*override.*commit\|per-verb-engine override keys exist for commit" internal/config/config.go` returns nothing; (3) the rewritten comments name the as-built `[commit].ai_command` / `[commit].timeout` per-verb overrides; (4) the `Commit` struct doc comment enumerates exactly the struct's current fields. `go vet`/`golangci-lint`/`gofmt` confirm the file still compiles and is well-formed.
- No existing test asserts the comment text, so no test pin changes; the existing config tests (Phase 1's `AICommandFor`/`TimeoutFor`/decode tests) must remain green, proving the comment edit did not perturb behaviour.

**Edge Cases**:
- Comment reflects as-built (per the task table) — no residual "Deliberately NOT added for commit" / no-per-verb-override claim; the comment must match the struct's actual fields after Phase 1 (read them before writing).
- External commit-command spec document revision stays out of scope (per the task table) — this task touches only `internal/config/config.go` comments; the spec-document edit is a separate commit-spec pass.

**Context**:
> Spec — Cross-spec reconciliation (commit spec): "Promoting per-verb `ai_command` formally reverses the commit-command spec's standing decision: 'Deliberately NOT added for commit … promote to a `[commit]` key only if a real need appears.' The real need has appeared, so that spec owes the reconciliation. This is not just a spec-doc edit: The as-built `Commit` struct doc comment in `internal/config/config.go` encodes the old 'no per-verb override' contract, and CLAUDE.md requires comments stay true to as-built in the same change."
> Spec — Cross-spec reconciliation (In-scope vs deferrable — resolved): "The code-level reconciliation is in scope for this work unit: the moment this work adds `[commit].ai_command` / `[commit].timeout`, the `Commit` struct doc comment encoding the old 'no per-verb override' contract must be updated in the same change (CLAUDE.md requires comments stay true to as-built). Planning must NOT defer the code/comment update — deferring it would ship a comment contradicting shipped code, exactly what CLAUDE.md forbids. Only the external commit-command spec document revision … may be handed to a separate commit-spec pass. Planning decides only that document-edit sequencing — not whether the in-repo comment update happens here (it does)."
> Phase 1 note (Task 1-3): "this task ADDS `[commit].ai_command`, which begins contradicting the existing `Commit` doc comment ('Deliberately NOT added for commit'). The CLAUDE.md same-change comment-reconciliation obligation for that doc comment is scoped to Phase 3 by the plan … do NOT edit the `Commit` doc comment here beyond what is mechanically required to compile; the full reconciliation is Phase 3's acceptance item." — this Task 3-3 IS that deferred full reconciliation.
> As-built: in `internal/config/config.go`, the `Config.Commit` FIELD comment (~lines 83–89) ends `No push/scope/per-verb-engine override keys exist for commit (spec: "Deliberately NOT added for commit").`; the `Commit` STRUCT doc comment (~lines 169–176) describes commit as carrying its two prompt knobs (Context, Prompt). After Phase 1 the `Commit` struct carries additional per-verb `ai_command`/`timeout` raw fields — read the current field list before rewriting so the doc comment matches the as-built fields exactly.
> CLAUDE.md (Comments): "Keep them TRUE TO AS-BUILT — when behaviour changes, the comment changes in the same commit. Never leave scope/phase claims that contradict the shipped code."

**Spec Reference**: `.workflows/ai-model-selection/specification/ai-model-selection/specification.md` — Cross-spec reconciliation (commit spec), including the In-scope vs deferrable resolution.
