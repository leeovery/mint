AGENT: duplication
FINDINGS:
- FINDING: Three near-identical AI-transport resolution helpers across the wiring sites
  SEVERITY: medium
  FILES: internal/engine/release.go:936-944 (aiTransport), internal/engine/regenerate_fresh.go:135-143 (resolveFreshTransport), internal/commit/run.go:780-788 (commitTransport)
  DESCRIPTION: The spec's three transport wiring sites were each implemented in isolation and converged on the same structure: an injected-transport test-seam short-circuit followed by an identical production construction `ai.NewTransport(r, ai.Config{AICommand: cfg.AICommandFor(verb), Timeout: cfg.TimeoutFor(verb)})`. The only varying token is the Verb constant (VerbRelease in two sites, VerbCommit in the third). The bodies are byte-for-byte identical apart from that constant and the receiver type of the deps wrapper. This is the classic copy-paste-across-task-boundaries pattern: the per-key sourcing contract ("source BOTH command and timeout from the accessor, assign Timeout directly so no zero-by-omission") now lives in three places and can drift independently — e.g. a future change that adds a third resolved field (or changes the no-deadline mapping) must be made identically in all three or one site silently diverges. The spec explicitly flags resolveFreshTransport as the "easy miss" site precisely because it is a structural duplicate of aiTransport.
  RECOMMENDATION: Extract the production-construction body into one shared helper that takes the runner, the resolved config, and the Verb — e.g. a `func buildTransport(r runner.CommandRunner, cfg config.Config, verb config.Verb) *ai.Transport` returning `ai.NewTransport(r, ai.Config{AICommand: cfg.AICommandFor(verb), Timeout: cfg.TimeoutFor(verb)})`. The two engine-package sites (aiTransport, resolveFreshTransport) can call it directly; commitTransport is in a different package, so either place the helper in a small shared location both import (it depends only on ai/config/runner, all already imported) or, at minimum, consolidate the two engine-package copies. Keep each site's nil-injected-transport test-seam guard local (that part legitimately differs by deps type); only the construction expression is the duplication to centralize. This makes the "thread command + timeout from the accessor, never zero-by-omission" contract live in exactly one place.

- FINDING: Duplicated deadline-recording CommandRunner spy across white-box transport tests
  SEVERITY: low
  FILES: internal/engine/release_aitransport_internal_test.go:26-50 (deadlineRunner), internal/commit/run_aitransport_internal_test.go:27-53 (deadlineCommitRunner)
  DESCRIPTION: The release and commit white-box transport tests each define their own full CommandRunner spy that records argv and whether the handed context carries a deadline (deadlineRunner vs deadlineCommitRunner). The two types are identical line-for-line except the type name — same four method implementations (RunWith records name/args/hadDeadline and returns a fixed non-empty body, Run delegates, RunInteractive/RunInDir are no-ops) and the same `durationPtr` helper is redeclared in both files. The regenerate internal test (regenerate_fresh_aitransport_internal_test.go) correctly REUSES the engine package's deadlineRunner, proving one spy suffices per package; the cross-package commit copy is the divergence. Each is ~25 lines, so this clears the rule-of-three / proportionality bar for a recording fixture.
  RECOMMENDATION: These live in separate test packages (engine internal vs commit internal), so they cannot share a file directly. If the project has (or wants) a shared runner test-double home, hoist a single deadline-recording spy there and have both packages use it. If a shared test helper package is not desired, this is acceptable as-is given the package boundary — flagging it as low because the cost is a real but bounded ~25-line copy with a redeclared durationPtr, and the regenerate test already demonstrates the intra-package reuse pattern.

SUMMARY: The three independently-authored transport wiring sites converged on a byte-identical production-construction expression differing only by the Verb constant — the main consolidation target so the per-key sourcing contract lives in one place. A secondary, lower-impact copy is the deadline-recording test spy duplicated between the engine and commit white-box tests.
