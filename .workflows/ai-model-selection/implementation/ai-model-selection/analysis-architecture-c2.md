AGENT: architecture
FINDINGS:
- FINDING: Per-site transport-resolution wrappers repeat the inject-or-build short-circuit three times
  SEVERITY: low
  FILES: internal/engine/release.go:933-938, internal/commit/run.go:777-782, internal/engine/regenerate_fresh.go:133-138
  DESCRIPTION: The new aitransport.New helper correctly consolidates the construction EXPRESSION (the ai.NewTransport(r, ai.Config{AICommand: cfg.AICommandFor(verb), Timeout: cfg.TimeoutFor(verb)}) call), but each of the three wiring sites still carries a near-identical wrapper — aiTransport, commitTransport, resolveFreshTransport — that all do the same two-line thing: "return the injected test transport when non-nil, else aitransport.New(...) for this verb". The only true variation is (a) the deps/arg wrapper type the transport is read from and (b) the verb constant. This is a deliberate, well-documented choice (the doc comments explain the deps wrapper differs per site so the short-circuit stays local), and it is correct — not a defect. It is recorded only as a minor residual: the inject-or-default polymorphism is the part that genuinely repeats, and the per-site wrappers exist because the injected-transport seam field lives on three different struct/param shapes (ReleaseDeps.Transport, Deps.Transport, a bare notes.Transport param). Because the divergence is structural (not just the verb), extracting it further would require unifying those three seam shapes, which is out of this work unit's scope and not worth the churn. No action needed; flagged for completeness only.
  RECOMMENDATION: Leave as-is for this work unit. If a future change unifies how the three call sites carry their optional injected transport, the inject-or-build branch could collapse into the shared helper too; until then the construction-expression consolidation already captures the part that mattered (the never-zero-by-omission timeout + per-key resolution contract lives once).
SUMMARY: The implementation is architecturally sound — the dedicated aitransport package is a correctly-placed, cycle-free seam (config never imports ai; ai never imports config; engine/commit are siblings that cannot host shared code), the *time.Duration boundary plus inverse-polarity NewTransport mapping is self-contained and keeps "no deadline" reachable only via an operator's explicit 0, the closed config.Verb enum makes regenerate routing un-missable by construction, defaults live canonically in config with every other site deriving from them, and integration coverage is exhaustive at all three wiring sites and end-to-end. The single recorded item is a low-severity, deliberate residual (per-site inject-or-build wrappers) with no action required this work unit.
