AGENT: standards
FINDINGS:
- FINDING: As-built comments carry forward-looking phase/task narration that contradicts shipped code
  SEVERITY: medium
  FILES: internal/config/verb.go:5, internal/config/config.go:82, internal/config/config.go:91, internal/config/config.go:104, internal/config/config.go:137, internal/config/config.go:199, internal/config/config.go:206, internal/config/config.go:210, internal/config/config.go:248, internal/config/config.go:259, internal/config/config.go:584, internal/config/config.go:589, internal/config/config.go:649, internal/config/config.go:695
  DESCRIPTION: CLAUDE.md's Comments contract is explicit: "Keep them TRUE TO AS-BUILT" and "Never leave scope/phase claims that contradict the shipped code." Several WHY-comments in the shipped files speak of work that is in fact completed in THIS change as still-future or deferred to a later task/phase, which now contradicts the as-built code:
    - verb.go:5 — "the layered accessors (AICommandFor / TimeoutFor, arriving in later tasks) accept" — both accessors are shipped in this same change (config.go), so they are not "arriving in later tasks."
    - config.go:82 — DefaultAICommand: "the transport's duplicate self-default and initgen's scaffold literal are removed/sourced from this constant in later phases" — both are already done in this change (transport self-default deleted; initgen pinned by a build-failing drift test). The "in later phases" framing reads as not-yet-done.
    - config.go:91 — DefaultTimeout: "the transport's defaultTimeout literal (which Phase 2 deletes in favour of this)" — that literal is already deleted in this change.
    - config.go:584,589 — TimeoutFor doc: "Phase 2's ai.Config.Timeout is also *time.Duration ..." and "that conditional lives in Phase 2, NOT here" — that boundary type and the transport's conditional WithTimeout skip are both shipped in this change (transport.go), so "lives in Phase 2" describes shipped code as future.
    - config.go:137,206,210,259,649 — multiple comments defer the negative-drop / floor application to "Task 1-7's TimeoutFor accessor" / "1-7's job" as if 1-7 were a separate future task; TimeoutFor is implemented in the same file in this change.
    - config.go:199,248,695 and config.go:104 — "the resolver (1-4) needs ..." / "engine-level keys and other verb tables arrive in later phases" — these phrase already-shipped resolution and the [commit] table as forthcoming.
  These are scope/phase claims contradicting the shipped code — precisely what CLAUDE.md forbids — even though every behavioural decision they describe is correctly implemented. (Distinct from the genuinely-historical "Phase 1" notes and the out-of-scope "Phase 6 provider validation" carve-outs, which remain accurate and should be left alone.)
  RECOMMENDATION: Rewrite the affected comments in the present tense / as-built voice: state the contract the code now upholds rather than which task or phase will deliver it (e.g. "AICommandFor / TimeoutFor resolve the layered chain" instead of "arriving in later tasks"; "the transport carries no default — this constant is the single source" instead of "removed in later phases"; "TimeoutFor drops a negative to the floor" instead of "the negative-drop is 1-7's job"). Keep the WHY/contract content; drop the task/phase tense.
SUMMARY: The implementation conforms to the specification on every substantive decision — pinned `claude -p --model sonnet` default sourced canonically from config, two-level per-key independent resolution (verb -> shared -> floor), `timeout = 0` honored as no-deadline with WithTimeout skipped, negative/invalid drop-through to the 60s floor, the closed two-value Verb enum with no `regenerate` member, all three wiring sites (including the easy-miss regenerate-through-[release] site) sourcing command+timeout from the accessors, the nil-Timeout panic guarding zero-by-omission, the initgen scaffold pinned to the config constants by drift tests, full README documentation, and the reconciled Commit struct comment — all verified by passing tests, clean build/vet/gofmt. The only standards drift is medium: forward-looking phase/task narration in several config/verb WHY-comments now contradicts the as-built code, violating CLAUDE.md's "comments stay true to as-built / no scope-phase claims that contradict the shipped code" rule.
