AGENT: duplication
FINDINGS:
- FINDING: `deadlineRunner` context-deadline spy duplicated across four test files
  SEVERITY: medium
  FILES: internal/aitransport/aitransport_test.go:25-54, internal/engine/release_aitransport_internal_test.go:26-46, internal/commit/run_aitransport_internal_test.go:24-53, internal/engine/regenerate_fresh_aitransport_internal_test.go (consumes the engine copy)
  DESCRIPTION: This work introduced a new test double — a `runner.CommandRunner` spy that records the invoked argv and whether the handed-in context carries a deadline (`_, d.hadDeadline = ctx.Deadline()`), returning a fixed non-empty body. It exists because the standard `runner.FakeRunner` discards the context, so it cannot observe the timeout-vs-no-deadline behaviour these tests assert. The identical ~25-line struct plus its four method implementations (`RunWith`/`Run`/`RunInteractive`/`RunInDir`) was authored four times: `aitransport_test.go` and `release_aitransport_internal_test.go` are byte-identical (`type deadlineRunner`), `run_aitransport_internal_test.go` is the same code renamed to `deadlineCommitRunner`, and `regenerate_fresh_aitransport_internal_test.go` reuses the engine package's copy. Three independent copies of the same non-trivial spy is a copy-paste-drift hazard: a change to the seam (e.g. a new `CommandRunner` method, or making the spy record stdin) must be made in three places and can silently diverge. The comment block "The FakeRunner discards the context, so these proofs use a tiny deadline-recording runner" is itself copy-pasted verbatim across all four, confirming independent authoring of the same concept.
  RECOMMENDATION: Extract the spy once into a shared test-support location and have all four sites consume it. The cleanest home is the existing `runner` package's test-double surface (alongside `FakeRunner`) as an exported `DeadlineRecordingRunner` (or similar), since it is a generic `CommandRunner` instrumentation with no engine/commit/aitransport knowledge — every caller already imports `runner`. The `aitransport_test.go` copy could then be deleted, the two `engine` internal copies collapsed to one shared reference (they are in the same package, so `deadlineRunner` need only be declared once there regardless), and the `commit` copy replaced. Do not add behaviour — extract the existing spy verbatim.

- FINDING: `durationPtr` test helper redeclared in three packages
  SEVERITY: low
  FILES: internal/aitransport/aitransport_test.go:54, internal/engine/release_aitransport_internal_test.go:131, internal/commit/run_aitransport_internal_test.go:53
  DESCRIPTION: The one-line helper `func durationPtr(d time.Duration) *time.Duration { return &d }` was authored independently in three of the new test files (and the equivalent `ptrTo` already existed pre-work in internal/ai/transport_test.go). These exist because `config.Config.Timeout` and `ai.Config.Timeout` are now `*time.Duration`, so tests need pointer literals. This is a trivial three-line helper, so by the Rule of Three it is at the borderline — but it travels with the `deadlineRunner` spy in the same files, so consolidating it alongside the spy extraction is essentially free and keeps the new timeout test ergonomics in one place rather than re-typed per package.
  RECOMMENDATION: Fold `durationPtr` into the same shared test-support extraction as the `deadlineRunner` spy (e.g. export it next to the shared runner spy, or as a small shared test helper). Within the `engine` package the two internal copies collapse to one declaration for free. Treat this as a rider on the spy fix, not a standalone refactor — do not extract it in isolation.

SUMMARY: The production wiring is already well-consolidated — all three transport-construction sites (release.go, regenerate_fresh.go, run.go) correctly route through the single `aitransport.New` helper, and the per-key resolution lives once each in `config.AICommandFor`/`TimeoutFor`, so the production side shows no duplication. The duplication is confined to test scaffolding: a non-trivial context-deadline `CommandRunner` spy was independently re-authored in three packages (medium) and a `durationPtr` pointer helper in three (low), both extractable into one shared test-support home.
