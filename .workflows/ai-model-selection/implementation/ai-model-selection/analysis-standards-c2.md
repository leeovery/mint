AGENT: standards
FINDINGS:
- FINDING: initgen re-types the pinned default literals instead of sourcing them from the config constant
  SEVERITY: low
  FILES: internal/initgen/initgen.go:16-22, internal/initgen/initgen.go:47-48, internal/initgen/initgen_test.go:57-93
  DESCRIPTION: The spec states twice — in "Single source of truth for config defaults" (line 126: "`initgen` pulls its template values from `config` rather than re-typing literals (the pinned default *value* is sourced from the config constant, not re-typed)") and in "Init template scaffolding" (line 137: "the pinned default *value*, **sourced from the config constant, not re-typed**") — that the init template's `ai_command`/`timeout` values must be sourced from the config constants, not re-typed. The implementation deliberately diverges: initgen.go does NOT import config and hard-codes the literals `'claude -p --model sonnet'` and `60` directly in the template string, justifying the choice in its package doc ("initgen does NOT import config … but it is not needed"). The drift is mitigated — initgen_test.go pins each literal equal to config.DefaultAICommand / config.DefaultTimeout (TestScaffoldedAICommand…/timeout… at lines 57-93), so a schema/scaffold drift fails the build rather than shipping a stale default — and the de-duplication intent (one canonical source, no silently-stale scaffold) is preserved by a different mechanism. But the spec's explicit, repeated decision was to source the value from the constant, and the implementation chose a re-typed-plus-drift-test approach instead. The functional outcome (no stale default ships) matches; the mechanism the spec named does not.
  RECOMMENDATION: Either (a) source the template values from config.DefaultAICommand / config.DefaultTimeout at template-build time (importing config, as the spec's literal wording directs — note config does not import initgen, so no cycle), or (b) if the team prefers the static-template approach with the drift-test pin (a defensible reading, since the spec also calls exact comment wording "a planning/impl detail" and the no-stale-default invariant holds either way), record that deviation explicitly so the spec text and the as-built no longer contradict. The drift test already guarantees correctness, so this is a spec-conformance/documentation gap, not a correctness bug.
SUMMARY: The implementation conforms to the specification on every load-bearing decision point — pinned default, per-verb ai_command/timeout resolution (verb → shared → floor), per-key independence, timeout=0 no-deadline with WithTimeout skip, negative-drops-through, the closed two-value Verb enum, all three wiring sites sourcing from the per-verb accessors via the shared aitransport.New helper, regenerate routing through [release] (the easy-miss site is argv-asserted to use [release] and not [commit]), the Commit struct and transport doc-comment migrations, and full README documentation; all gates (build, gofmt, vet, test -race, golangci-lint) pass clean. The single divergence is low-severity: initgen re-types the pinned default literals and pins them via drift tests rather than sourcing them from the config constant as the spec's "sourced from the config constant, not re-typed" wording directs — the no-stale-default invariant is preserved by the drift test, so it is a spec-mechanism/documentation mismatch, not a correctness defect.
