AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the AI-model-selection specification and project conventions. Every spec decision is present and correct: the pinned default (config.DefaultAICommand = "claude -p --model sonnet"), the both-level ai_command/timeout layering with verb→shared→floor resolution centralized in config.AICommandFor / config.TimeoutFor, the closed two-value config.Verb enum with no regenerate member (verb.go) routing regenerate_fresh.go through VerbRelease, the net-new integer-seconds timeout key with absent-vs-explicit-zero preserved via *time.Duration to the transport boundary, the transport's deleted self-defaults with the timeout=0⇒skip-WithTimeout conditional, the zero-by-omission guard (NewTransport panics on nil Config.Timeout — an idiomatic programmer-error panic, not an expected condition, so consistent with golang-error-handling rule 9), the single aitransport.New seam threading all three wiring sites, the initgen template surfacing the new keys with model-agnostic comments sourced/pin-tested against the config constants, the same-change Commit-struct doc-comment reconciliation reversing the old "no per-verb override" contract, and the full README documentation of all six required points. All spec acceptance behaviors (per-key independence, ai_command-never-empty, timeout=0 honored, negative/invalid drop-through, regenerate-routes-through-release, zero-config pinned default) have direct, exact-assertion test coverage. build passes, gofmt clean, and no stale defaultAICommand/defaultTimeout literals or un-migrated transport doc comments remain.
