AGENT: standards
FINDINGS:
- FINDING: Procedure step 2 phrasing is ambiguous about WHICH README to read
  SEVERITY: low
  FILES: internal/setupguide/setupguide.go:112-114
  DESCRIPTION: The spec's setup procedure step 2 (specification.md "Emitted guide — setup procedure", step 2: "Learn mint — read the README and internalise mint's minimalist philosophy") refers to mint's OWN README — the human config-reference surface and the source of mint's commands/philosophy. The emitted guide renders this as "Read the project's README for mint's commands and surface". "the project's README" most naturally reads as the TARGET project's README, which would not document mint's commands or philosophy. Because the guide prose is the deliverable's load-bearing content (it drives the AI's behaviour and is the one part with no compiler, per the spec's "Acceptance — prose quality" note), an instruction the agent could misread to inspect the wrong README is a genuine, if minor, drift from the decided intent. No test catches this (the structural tests key on markers, not this prose).
  RECOMMENDATION: Reword step 2 to name mint's README unambiguously, e.g. "Read mint's README (the human config reference)" so the agent does not conflate it with the target project's README. Keep the minimalism-philosophy clause intact.
SUMMARY: The implementation conforms closely to the specification — the mint setup subcommand, the five marked guide sections, the config-metadata SoT with its decided default-column convention, the (level, key) bijection drift test (recurse-don't-count for containers, dual-level keys as distinct rows), the stripped-to-minimal initgen template with both header pointers, the help-contract coverage, and the README reconciliation are all present, drift-tested, and pass build/gofmt/vet/test/golangci-lint. Project seams (no presenter/runner/os-exec bypass; setupguide imports config in exactly one render path; setup runs unconditionally with no repo guard) are respected. The only drift is one ambiguous prose line in the emitted guide's procedure step 2.
