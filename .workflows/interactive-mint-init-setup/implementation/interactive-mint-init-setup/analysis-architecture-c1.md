AGENT: architecture
FINDINGS:
- FINDING: "shared" token is overloaded across the Level and Default columns of the same rendered table
  SEVERITY: medium
  FILES: internal/setupguide/setupguide.go:347-352 (levelCell), internal/config/metadata.go:135-136,150-151 (per-verb override Default), internal/config/metadata.go:112-114 (shared-level rows)
  DESCRIPTION: The config-reference table that mint setup emits surfaces the word "shared" with two unrelated meanings in two columns of the SAME table. levelCell() renders the empty LevelShared form as the literal word "shared" in the LEVEL column (its stated rationale: a blank markdown cell is ambiguous). Separately, the per-verb override rows ([release]/[commit] ai_command/timeout) carry "shared" in the DEFAULT column to mean "inherit the shared value." So the shared-level ai_command row reads `| ai_command | shared | claude -p --model sonnet | ... |` (shared = level) while the [release] ai_command row reads `| ai_command | [release] | shared | ... |` (shared = inherit). A reading agent scanning the table sees the same word meaning "this key lives at the top level" in one cell and "this key defaults to whatever the top-level value is" in an adjacent cell. The spec made the Default-column "shared" token a load-bearing decided convention (minimalism depends on the default column being unambiguous, per spec "default column representation"); the Level-column "shared" placeholder is a render-site invention added in levelCell, NOT a spec convention, and it was not reconciled against the existing meaning. The README — which the SoT header claims to keep "mutually consistent" — avoids the collision entirely because it has no single combined table with a level column (it uses separate `### Shared engine keys` / `### [release]` section tables, no level column). The single-combined-table shape is unique to the agent surface, and that is exactly where the overload bites.
  RECOMMENDATION: Render the shared level with a token that does not collide with the inherit-default token — e.g. "top-level" or "(top level)" in levelCell — keeping the value driven by MetadataLevel.String() (placeholder only when String() is empty). The fix is confined to levelCell() and its mirror in setupguide_test.go; the SoT Default column convention stays untouched.

- FINDING: blank-default rendering is asserted vacuously — the "renders blank" integration check always passes
  SEVERITY: low
  FILES: internal/setupguide/setupguide_test.go:43-53 (containsRowLine), internal/setupguide/setupguide_test.go:287-317 (TestGuide_ConfigReferenceCarriesDefaultTokensVerbatim), internal/setupguide/setupguide.go:358-363 (defaultCell)
  DESCRIPTION: defaultCell() turns an empty SoT default into a single space so the markdown cell stays well-formed — a deliberate render decision the spec's blank/auto/[]/shared convention rests on. But no test pins that behaviour. containsRowLine() checks the default via strings.Contains(line, defaultCell), and for a blank-default key (e.g. [release].context) the passed defaultCell is "", so strings.Contains(line, "") is unconditionally true. The "empty-string [release].context renders blank" subtest in TestGuide_ConfigReferenceCarriesDefaultTokensVerbatim therefore proves only that the key/level/description appear on a line — it asserts NOTHING about the blank rendering it claims to cover. The blank-vs-auto-vs-shared distinction is called out as load-bearing for the agent (the config package's metadata_test.go pins it hard on the SoT side via TestMetadataRows_SentinelAutoDefaultsRenderAuto's explicit not-blank assertion), but the seam where it actually reaches the agent — the rendered table — has no real coverage for the blank case. A regression that emitted "auto" or dropped the cell delimiter for a blank-default key would not be caught by this subtest.
  RECOMMENDATION: Give the blank case a real assertion that does not collapse to Contains(_, ""): assert the rendered line for a blank-default key contains the exact "| | " (single-space) cell sequence defaultCell produces, or add a defaultCell unit test pinning defaultCell("") == " " and assert the column count / delimiter shape on a blank row. Keep the non-blank rows driven by row.Default as today.

SUMMARY: The new config-metadata SoT, its mechanical bijection drift test, and the mint setup / mint init / mint help / README render-target split are cleanly layered and well-seamed — package boundaries, the no-config-import initgen seam, and the cmd-layer setup wiring all hold. Two issues sit at the agent-facing render seam: a "shared" token overloaded between the Level and Default columns of the emitted table (medium), and a vacuous blank-default assertion that leaves the load-bearing blank/auto/shared distinction unverified at the render site (low).
