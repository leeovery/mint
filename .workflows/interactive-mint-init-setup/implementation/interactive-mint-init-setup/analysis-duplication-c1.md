AGENT: duplication
FINDINGS:
- FINDING: Production levelCell helper re-implemented verbatim in the setupguide test
  SEVERITY: medium
  FILES: internal/setupguide/setupguide.go:347-352, internal/setupguide/setupguide_test.go:17-22
  DESCRIPTION: The production levelCell(config.MetadataLevel) string in setupguide.go and the test-local levelCell in setupguide_test.go are byte-identical: both return level.String(), falling back to the literal "shared" when String() is empty. The test re-implements the exact placeholder logic it is meant to verify rather than exercising the production behaviour. This is a self-fulfilling test — if the production "shared" placeholder convention ever changes (e.g. to a different token, or to an em-dash), the test copy would have to be hand-edited in lockstep; until it is, the two copies drift silently and TestGuide_ConfigReferenceHasLinePerSoTRow / TestGuide_ConfigReferenceRendersLevelViaString keep passing against the test's own re-derivation instead of catching a real regression. The render's level-placeholder convention is load-bearing (a blank markdown cell is ambiguous to the reading agent), so the duplication sits exactly on a contract the tests are supposed to pin.
  RECOMMENDATION: Remove the test-local levelCell and have the test assert against the production behaviour instead — either export the helper (e.g. setupguide.LevelCell, or a small exported render seam) and call it from the test, or assert the rendered table line directly against config.MetadataLevel.String() with the "shared" fallback inlined in the assertion's expectation. The goal is one source for the placeholder convention so the test fails when production drifts, not tracks it.

- FINDING: rowKey type plus index-by-(level,key) helper duplicated across the config and setupguide test suites
  SEVERITY: low
  FILES: internal/config/metadata_test.go:14-33, internal/setupguide/setupguide_test.go:336-353
  DESCRIPTION: Both files define an identical unexported rowKey struct ({level config.MetadataLevel; key string}) and a near-identical helper that folds config.MetadataRows() into a map[rowKey]config.MetadataRow keyed on the (level, key) pair (rowSet in metadata_test.go, rowByLevelKey in setupguide_test.go). The two were written independently against the same SoT shape and now carry the same lookup idiom in two packages. They diverge only cosmetically: rowSet additionally fails on a duplicate (level, key) pair, whereas rowByLevelKey silently last-wins. Because they live in separate test packages (config_test vs setupguide_test) this cannot be a single shared symbol without a test-support package, so the cost of consolidation is real; the impact is correspondingly low (test-only, ~15 lines each). Worth recording as copy-paste drift rather than fixing aggressively.
  RECOMMENDATION: If a shared test-support package is later introduced for config metadata fixtures, host one rowKey + one index helper there (carrying rowSet's duplicate-pair guard, the stricter of the two). Absent that, leave as-is — extracting a cross-package test helper for two call sites is not yet proportional. Do NOT consolidate the config-package metadata_test.go rowKey/rowSet with metadata_drift_test.go's leafKey/leafKeySet: those are deliberately independent per the spec's bijection design (the SoT side and the schema-reflection side must derive from different sources), so merging them would defeat the drift test.

SUMMARY: One medium finding — the setupguide test re-implements the production levelCell placeholder logic verbatim, so it tracks rather than guards that convention. One low finding — a rowKey type and (level,key) index helper independently re-authored in the config and setupguide test suites. The config package's intentionally-independent SoT-vs-schema bijection helpers (rowKey/rowSet vs leafKey/leafKeySet) and the lightly-duplicated README config reference are by-design per the spec and are NOT flagged.
