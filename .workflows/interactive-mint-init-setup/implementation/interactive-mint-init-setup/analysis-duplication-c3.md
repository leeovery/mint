AGENT: duplication
FINDINGS:
- FINDING: Shared ai_command / timeout default value is pinned redundantly across overlapping metadata_test.go tests, with two different timeout-seconds derivation spellings
  SEVERITY: low
  FILES: internal/config/metadata_test.go:309-320 (SharedAICommandDefaultEqualsConfigConstant), internal/config/metadata_test.go:329-341 (SharedTimeoutDefaultEqualsConfigConstant), internal/config/metadata_test.go:348-362 (SharedTimeoutDefaultIsIntegerSecondsNotDuration), internal/config/metadata_test.go:371-398 (ConcreteScalarDefaultsRenderVerbatim)
  DESCRIPTION: The shared-level ai_command and timeout default cells are each asserted equal to their config constant by a dedicated single-key test AND again inside the table-driven ConcreteScalarDefaultsRenderVerbatim test (lines 379-380), so the same SoT->constant pin is duplicated. The timeout assertion is also spelled two different ways for the same value: the dedicated tests derive seconds as `int(config.DefaultTimeout / time.Second)` (line 337) while the table-driven test derives it as `strconv.Itoa(int(config.DefaultTimeout.Seconds()))` (line 380, a float64 truncation). Two derivations of one drift-pin can quietly diverge (e.g. a sub-second DefaultTimeout would truncate differently) and a future reader cannot tell which is canonical. This is the only same-concept-different-spelling drift I found; it sits just below the Rule-of-Three extraction bar but the divergent spelling is the real concern, not the line count.
  RECOMMENDATION: Pick ONE seconds derivation (match the production SoT spelling in metadata.go:128, `int(DefaultTimeout / time.Second)`) and use it in both timeout assertions; drop the shared ai_command and shared timeout rows from ConcreteScalarDefaultsRenderVerbatim's table since their constant-pin is already owned by the two dedicated SharedXEqualsConfigConstant tests, leaving ConcreteScalarDefaultsRenderVerbatim to cover only the literal-default keys (max_diff_lines, tag_prefix, commit_prefix, publish, changelog, on_notes_failure) it uniquely guards.
SUMMARY: The implementation is unusually well-consolidated — the work unit deliberately extracted the (level,key) indexer into the configtest package, merged the triplicated expected-pairs census into metadata_census_test.go, and single-sourced the tomlTag and LevelCell helpers, so the cross-file duplication those would have caused is already gone. The only residual duplication is one low-severity cluster of overlapping/divergently-spelled shared-default pins inside metadata_test.go. The new cmd-layer flag.ErrHelp and "mint: %v" stderr idioms in setup.go match pre-existing patterns in init.go/main.go/regenerate_all.go and are 1-3 line conventions below the extraction bar (and largely pre-existing code, out of plan scope).
