AGENT: standards
FINDINGS:
- FINDING: Shared ai_command SoT description diverges from README without the "verb → shared → default" alignment the spec asks the two surfaces to keep consistent
  SEVERITY: low
  FILES: internal/config/metadata.go:126, README.md:203
  DESCRIPTION: The spec ("Config-metadata source of truth" → `default` column representation; "README — config reference verification") decides the SoT and README per-key tables adopt the SAME representation convention so the two surfaces "stay mutually consistent," and explicitly accepts that "README descriptions may lightly duplicate the SoT." The two surfaces are consistent on the load-bearing `default` tokens (blank / auto / [] / shared / scalar) — verified across every key. The only divergence is incidental prose: the shared `ai_command` SoT description ends "resolved [verb] → shared → default" while the README writes "resolves `[verb]` → shared → default (see The AI Transport)". This is the spec-sanctioned light duplication, not a contract break — flagging only so a future editor knows the two descriptions are deliberately allowed to differ in wording while the default-column tokens must stay identical. No change required.
SUMMARY: The implementation conforms to the specification and project conventions across the board — pure string-emitter setupguide (seam-3 honoured, cmd-layer Fprint is the documented curated-text exception), unconditional no-guard `mint setup`, all five stable section markers, the SoT↔schema bijection drift test (25 leaf pairs, recurse-don't-count containers, per-level dual keys), the decided default-representation convention (blank/auto/[]/shared/scalar), the minimal initgen template with both header pointers (old commented-template tests removed), full help-contract wiring + coverage test, README reconciliation (minimal framing, embedded block replaced, Commands line corrected, tripwire test), and a frozen `mint help` with no config reference. All gates pass (build, gofmt, vet, test, lint = 0 issues). The single finding is a low-severity, spec-sanctioned prose duplication, not drift.
