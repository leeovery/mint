AGENT: duplication
FINDINGS:
- FINDING: Container-tag set duplicated verbatim across two tests in the same package
  SEVERITY: low
  FILES: internal/config/metadata_test.go:91, internal/config/readme_tripwire_test.go:156
  DESCRIPTION: TestMetadataRows_NoContainerRows and TestDistinctSchemaKeyNames_ExcludesContainerTags each open with the byte-identical literal `containers := map[string]bool{"release": true, "commit": true, "hooks": true}` and then iterate asserting no row/name is a container tag. Both files are `package config_test`, so they compile into the SAME test binary — the literal is shareable but currently exists as two copies that must be kept in lockstep if the schema ever grew a fourth sub-table. This is exactly the "duplicate list that could silently diverge" trap the team already eliminated for the expected-pair census (metadata_census_test.go) and the (level, key) indexer (configtest). A third copy of the same fact (the closed set of container tags) lives at metadata_drift_test.go:39 as `containerLevels` (a differently-typed `map[string]MetadataLevel` in `package config`); that one is the authoritative producer-side map and is legitimately separate (different package binary, different shape, drives the recursion). The two config_test copies are the avoidable pair.
  RECOMMENDATION: Hoist the container-tag set to a single shared helper in the config_test package (e.g. a small `func isContainerTag(string) bool` or a package-level `var containerTags = map[string]bool{...}` in one of the two _test.go files) and have both tests call it, mirroring how the expected census and the row indexer were already consolidated. Do not try to also fold in metadata_drift_test.go's `containerLevels` — it is in a different package and carries level mappings, not a plain presence set.

- FINDING: Near-duplicate markdown-row parsing helpers in the setupguide structural test
  SEVERITY: low
  FILES: internal/setupguide/setupguide_test.go:39 (containsRowLine), internal/setupguide/setupguide_test.go:72 (blankDefaultCellRenders), internal/setupguide/setupguide_test.go:368 (lineHasKeyAtLevel), internal/setupguide/setupguide_test.go:527 (rowLineFor), internal/setupguide/setupguide_test.go:538 (levelCellOf), internal/setupguide/setupguide_test.go:548 (splitRow)
  DESCRIPTION: Six helpers in one test file each parse the same fixed `| key | level | default | description |` markdown row shape, but via three independent splitting strategies: containsRowLine/lineHasKeyAtLevel use substring Contains across the whole line; blankDefaultCellRenders does its own `strings.Split(line, "|")` and indexes field 3 with a hand-counted six-field expectation; rowLineFor/levelCellOf/splitRow do a separate `strings.Split(line, "|")` with trimming and leading/trailing-field dropping. The two raw `strings.Split(line, "|")` implementations encode the same delimiter contract (a leading "| ", " | " between columns, a trailing " |") with different off-by-one conventions (six raw fields incl. empties vs trimmed cells with ends dropped). Because they were written from different angles they can drift if the render delimiter ever changes — a delimiter tweak in renderConfigReference would need fixing in two places that look unrelated. This is below the rule-of-three pain threshold for production code, but it is a maintenance seam worth noting.
  RECOMMENDATION: Consolidate row parsing onto the single splitRow primitive (it already returns trimmed per-column cells). Have blankDefaultCellRenders, containsRowLine, and lineHasKeyAtLevel locate the row via splitRow and assert on indexed cells rather than re-splitting or scanning the whole line — keeping the raw-field blank-cell check only where the single-space shape is genuinely load-bearing. One parser means a future delimiter change is a one-line fix.

SUMMARY: The implementation is notably clean on duplication — the highest-value extractions (the (level, key) row identity + indexer into configtest, the expected-pair census into metadata_census_test, the tomlTag primitive) were already done during construction. Only two low-severity residual seams remain: a verbatim container-tag map duplicated across two same-package config tests, and a cluster of independently-written markdown-row parsers in the setupguide test that could share one splitter.
