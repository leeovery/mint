AGENT: standards
FINDINGS: none
SUMMARY: Implementation conforms to the specification and project conventions. The thin `mint setup` feature, the config-metadata SoT with its schema-derived bijection drift test, the strip-to-minimal `initgen` template, the help-surface wiring, and the README reconciliation all match the decided behaviour; the gates (build, gofmt, vet, tests, golangci-lint) pass clean.

NOTES (verified, no drift):
- mint setup runs unconditionally with no git/cwd guard, no presenter/engine wiring, and the only IO is a cmd-layer fmt.Fprint to stdout — exactly the documented CLAUDE.md seam-3 exception for curated help/usage text. The cwd-confirm safety net lives in the emitted guide as the spec mandates (setup.go:23-48, setupguide.go procedure step 1).
- All five required stable section markers are emitted (pipeline, etiquette, minimalism, existing-config, config-reference), each on its own line, keyed by exported constants the structural test greps — not prose (setupguide.go:45-60; setupguide_test.go).
- The SoT default-column representation convention is honoured per the spec: blank for empty-string defaults, "auto" for sentinel-empty release_branch/provider, "[]" for diff_exclude, "shared" for per-verb ai_command/timeout overrides, blank for activate-only [release.hooks] keys; shared ai_command/timeout/max_diff_lines sourced from the exported config constants so they cannot drift (metadata.go:119-167; pins in metadata_test.go). These tokens match the README per-key tables (README.md:201-242), satisfying the mutual-consistency requirement.
- The drift test is a total bijection over leaf (level, key) pairs derived mechanically from the unexported decode-shape toml tags (fileShape/releaseShape/commitShape/hooksShape) with recurse-don't-count for container fields and dual-level keys as distinct rows — exactly the decided contract (metadata_drift_test.go, metadata_bijection_test.go).
- initgen MintTOML() is the only changed initgen surface: empty body + header carrying both pointers (GitHub docs + `mint setup`), the cold-arrival recovery net; ReleaseShim untouched; commented-template assertions removed and replaced by minimal-shape + header-pointer pins (initgen.go, initgen_test.go). initgen still does not import config.
- Help-surface wiring complete: rootUsage setup line states it PRINTS/EMITS (not writes), curated setupUsage notes no flags beyond --help and runs anywhere, commandSetup dispatch route, unknown-command message lists `mint setup`, and the usage-coverage test pins the setup verb (usage.go, main.go, usage_test.go). mint help carries no config reference (guarded by TestRootUsage_CarriesNoConfigReference).
- README Configuration + Commands sections reconciled with strip-to-minimal: framing corrected to "minimal" .mint.toml, the embedded full-template block replaced by the minimal template, per-key tables remain the authoritative human reference, and the optional tripwire test asserts every distinct schema key name appears in the README (README.md:182-244, readme_tripwire_test.go).
- The light README↔SoT prose duplication is the spec-accepted "config reference verification" surface and is not flagged.
