AGENT: standards
FINDINGS:
- FINDING: README "## Commands" section omits a `setup` entry while listing every other verb
  SEVERITY: low
  FILES: README.md:66-180
  DESCRIPTION: The spec frames `mint setup` as "a new top-level verb" threaded through mint's help surface "exactly like every other verb" and is explicit that the rootUsage command list gains a `setup` line (delivered correctly in cmd/mint/usage.go:22). The README's `## Commands` section documents every other verb in its own `### `-headed block (`init`, `release`, `release regenerate`, `commit`, `version`, `help`) but has no `### setup` entry — setup is only described under the Quick Start "### Setup" subsection (README.md:58-64). This is a documentation-surface inconsistency, not a hard spec violation: the spec's README requirements (entry-point prompt + Configuration reconciliation) are all satisfied, and the spec does not explicitly mandate a Commands-section setup block. Flagging because a reader scanning `## Commands` for the full verb surface will find setup conspicuously absent from the otherwise-complete list, and the rootUsage/help surface DOES list it — the two human surfaces diverge.
  RECOMMENDATION: Optional. Consider adding a short `### setup` block under `## Commands` (mirroring the `### version` / `### help` brevity) that states it prints the AI-assisted setup guide and runs anywhere, so the README Commands surface matches the rootUsage command list. Keep the print/emit-not-writes wording the rootUsage line already pins (usage_test.go TestRootUsage_SetupDescriptionPrintsNotWrites).
SUMMARY: The implementation conforms closely to the specification and all CLAUDE.md seam contracts: mint setup is a pure stdout text emitter with no repo guard (seam-3 cmd-layer fmt.Fprint exception correctly invoked, no presenter/engine pulled in), the config-metadata SoT is schema-adjacent and drift-tested via a total (level,key) bijection mechanically derived from the decode-shape toml tags (recurse-don't-count containers, dual-level keys distinct, 25 leaf pairs), the SoT default-column representation tokens (blank / auto / [] / shared / verbatim scalar) match the decided convention and the README per-key tables, the shared ai_command/timeout cells are sourced from the exported config constants so the value-drift pin is genuinely subsumed, initgen MintTOML() is stripped to empty-body-plus-header with both cold-arrival pointers, the README Configuration section is reconciled to strip-to-minimal with the minimal template block replacing the old commented one, the guide's drift-sensitive pipeline/PONR content matches the engine as-built, every required guide section emits a stable greppable marker keyed by the structural test, and the help-coverage test is extended to pin mint setup. The single finding is a low-severity README Commands-section omission, not a behavioural or contract drift.
